# Do not commit an actual password or JWT secret.
DATABASE_URL=postgres://odh_app:change_me@127.0.0.1:5432/odh_sao
JWT_SECRET=replace_with_a_random_secret_of_at_least_32_characters
PORT=8787
# Comma-separated browser origins allowed to call the API.
ALLOWED_ORIGINS=http://127.0.0.1:8766,https://hainox.github.io
