# Журнал проекта

## 2026-09-15 — аудит централизованной фотофиксации

- Продолжена работа в отдельной копии karta-sao-photo-service. Проверены ветка photo-service-centralization, HEAD 841ce4f и статус до изменений; рабочая копия до создания документов была чистой. У ветки необычная tracking-настройка origin/main; её не меняли.
- Повторно проверено отсутствие dev/Vision.md, dev/BuildSpec.md и dev/ProjectLog.md на исходной базе. Vision не создавался: утверждённых постоянных принципов в копии нет, обязательные ограничения этой работы взяты из задания пользователя.
- Прочитан контрольный чекпойнт 2026-09-14 и исходные XLSX рядом с ним. Сверены встроенные объекты трёх карт с районными полигонами: 812 остановок (806 назначены, 6 вне полигонов); 2235 координат ПП, 387 odh_id, 427 назначенных пар odh_id + district, один odh_id с двумя точками остаётся без района; 35 других odh_id корректно образуют несколько пар по районам; 10035 входов, 3209 УНОМ. Уточнения и районные числа — в dev/Data-Audit.md.
- Все три публичные карты ответили HTTP 200. В проверенном браузере карта остановок имела подложку/маркеры, подложка ПП и подъездов не отрисовалась; постоянство и причина не доказаны. На всех трёх картах фото пока используют браузерную IndexedDB; отдельный API/БД новой фотослужбы в копии отсутствуют.
- Установлено, что текущий API и база проекта — другая служба для обмена ОДХ/точечных photo-markers. Её не запускали, не использовали её health endpoint или секреты. Наличие production-фотобазы внешне не проверялось.
- Проверено наличие существующего редактора маршрутов и памятки префектуры. Их не перезаписывали; проверены подписи карт и тестовые наборы регрессии.
- В старом API обнаружен скрипт демонстрационных аккаунтов; общий пароль 12345678 в тестовой фикстуре старого API. Не переносить в новую production-службу. Рекомендация OIDC/SSO либо индивидуальных приглашённых аккаунтов вынесена на утверждение.
- Подготовлены шесть вариантов интерфейса: три для ПК и три для телефона. Пользователь выбрал №2 для ПК и №5 для телефона. 2026-09-15 пользователь утвердил BuildSpec как объём и критерии. Подтверждены нормы фото 1/2/1, пороги <33% / 33–<66% / ≥66%, предложенная формула по подтверждённым фото и модель OIDC при наличии либо индивидуальные приглашённые учётки; районная роль ограничена своим районом, префектурная видит сводку САО. Доступные Color Designer – Palette Maker и Supericons использованы для предварительной палитры и поиска иконок. Product Design и Agent Skills изучены в доступных локальных инструкциях. В одной попытке Product Design preflight script/context отсутствовали, поэтому концепции построены только на пользовательском брифе и наблюдаемой странице.
- Retention: эталонные фото бессрочно, прочие — кандидаты на удаление при нехватке места. Порог нехватки, полномочия удаления, форматы/лимит файла, объём, backend и backup SLA уточняются. Семантика GPS ±5 неоднозначна. Районное назначение outliers обсуждается лично; устойчивость ключа подъезда между выгрузками не проверена. «Оба варианта» отчёта записано как экран + экспорт; требуется уточнить Excel/PDF и владельца процесса.
- Созданы dev/Data-Audit.md и утверждённый dev/BuildSpec.md; ProjectLog фиксирует утверждение и открытые решения. Разрешена локальная работа над независимыми от них частями. Исходная папка, действующее приложение, схема БД и сервер в аудиторском проходе не менялись.
- На момент аудита тесты кода не запускались; проверка нового health-каркаса ведётся отдельно. git push, deploy и SSH не выполнялись.
- ChangeLog.md не обновлялся: пользовательское приложение и совместимость не менялись.

## 2026-09-15 — изолированный локальный health-каркас фотослужбы

- Добавлен минимальный `photo-service`: Node.js 24 endpoint `GET /healthz`, который проверяет только подключение по `PHOTO_SERVICE_DB_*`; на ошибке возвращает общий 503 без деталей. Старый `/api/health` не используется и новой службой не предоставляется.
- Добавлены отдельные Compose-ресурсы: PostgreSQL `sao_photo_service`, собственные проект/сеть/том, отдельный пользователь и пароль фотослужбы; порт приложения публикуется только на loopback. Контейнер приложения read-only, без Linux capabilities и без install scripts для npm-зависимостей.
- Зафиксирована зависимость `pg` и создан `package-lock.json`. На этапе health-каркаса `npm test` — 14/14; `npm audit --omit=dev` — 0 уязвимостей; `docker compose config --quiet` прошёл.
- Добавлена чистая функция прогресса отчёта с утверждёнными фото-нормами 1/2/1, порогами `<33%`, `33–<66%`, `≥66%`, обработкой пустой выборки и валидацией счётчиков. Она не подключена к источнику объектов или интерфейсу и не решает открытый вопрос знаменателя по нераспределённым объектам.
- После добавления функции `npm test` — 14/14; границы 33% и 66% проверены отдельными тестами. `npm audit --omit=dev` до этой чистой логической правки показал 0 уязвимостей; зависимости не менялись.
- Выполнен одноразовый Docker smoke с новым случайным именем проекта, временным паролем, свободным loopback-портом и отдельным PostgreSQL: `/healthz` вернул `status=ok`, `database=connected`. После теста контейнеры, сеть и именованный том этого проекта удалены; незакрытых smoke-ресурсов не осталось.
- Результат подтверждает только локальную связность нового приложения с одноразовой тестовой PostgreSQL. Он не подтверждает схему фото, загрузку/выдачу, роли/аутентификацию, отчёты, резервное копирование, production-БД или готовность службы принимать реальные данные. Внешняя среда и старая БД не использовались.
- Добавлена чистая агрегация `summarizeCoverage` для уже выделенного набора объектов: нормы 1/2/1, «с фото/без фото», подтверждённое выполнение, частичные и pending-review объекты, отдельный счётчик георисков и процентная полоса. Pending-фото считаются наличием фото, но не подтверждённым выполнением; счётчики pending и partial независимы и могут пересекаться, чтобы очередь проверки не скрывалась. Helper не подключён к API/БД и не решает состав знаменателя САО.
- Красно-зелёный цикл для агрегации пройден: сначала тесты падали из-за отсутствующего `src/report.js` и группировки по типу, затем отдельно поймано и исправлено скрытие pending у завершённого объекта; весь набор `npm test` после исправления — 21/21.
- Добавлен чистый гео-helper `haversineDistanceMeters`/`assessDistanceRisk`: минимальная дистанция до зарегистрированной точки, риск при превышении 15 м, ручная проверка при отсутствии GPS/опорных точек и явная метка приближения вместо проверки контура. Политика допустимой точности «±5» намеренно не выводится автоматически.
- После добавления гео-helper весь набор `npm test` — 26/26; зависимости не менялись.
- `ChangeLog.md` не изменялся: пользовательский интерфейс, API контракты действующего приложения и совместимость не менялись. Push, deploy и SSH не выполнялись.

### Остаётся

- Уточнить GPS ±5, параметры файлов/media/backup, экспорт и владельца отчёта, доступность OIDC и результаты районного назначения outliers.
- До добавления фото-схемы и upload закрыть необходимые media/retention решения; также подтвердить GPS-буфер, форматы экспорта и владельца отчёта.
- После согласования параметров — подготовить отдельный локальный тестовый контур сервиса, реализовать серверную загрузку/выдачу и отчётность, интегрировать выбранные сценарии, обновить памятку и ProjectLog, выполнить серверные тесты и ручную проверку ПК/телефона.
- Production, удалённую БД и деплой не проверять до отдельного прямого разрешения.

## 2026-09-15 — чекпойнт перед переносом на другое устройство

- Пользователь утвердил индивидуальные учётные записи, обязательный GPS для отправки фото, допуск GPS ±5 м вокруг номинального радиуса 15 м, нормы 1/2/1 и два экспорта: полный Excel с метаданными/фотографиями и краткий PDF со сводкой.
- В рабочей копии реализован изолированный backend фотослужбы: миграция PostgreSQL `migrations/001_initial.sql`, scrypt-пароли и httpOnly-сессии, роли `district_editor`/`prefecture_admin`, проверка области района, multipart upload с idempotency key, MIME/magic-byte/20 МБ лимиты, отдельное файловое media-хранилище, ручная проверка фото, выдача фото по авторизованному запросу, summary/Excel/PDF endpoints и аудит действий.
- Импорт исходных карт `scripts/import-object-maps.js --dry-run` повторно дал 812 остановок, 2235 координат ПП, 10035 входов; сформировал 812 stop, 428 reportable PP (427 назначенных пар + 1 unassigned), 10035 entrance; 7 объектов остаются без района (6 остановок и один PP-odh_id).
- Все три standalone-карты переключены с IndexedDB на API фотослужбы: вход, серверные счётчики/галерея, обязательный GPS перед отправкой и повторяемая отправка. CSV-контур Яндекс-карт сохранён.
- Проверки: `npm test` — 34/34; inline JS трёх карт разобран без синтаксических ошибок; Docker Compose smoke с отдельными случайными именем проекта/паролем/томами прошёл: миграция 001 применена, `/healthz` вернул 200 `database=connected`, контейнер приложения работал от UID 1000 и имел writable только отдельный media volume; smoke-ресурсы удалены.
- В runtime-зависимостях добавлены `busboy`, `exceljs`, `pdfkit`, `pg`; npm install scripts отключены. `npm audit signatures` проходит, но `npm audit --omit=dev` сообщает 2 moderate advisory через декларацию `exceljs -> uuid <11.1.1`; установленный `uuid` принудительно 11.1.0, breaking `audit fix --force` не применялся. Перед production нужен отдельный security verdict по этому предупреждению.
- Production не развёрнут и SSH не выполнялся: для безопасного rollout ещё нужны проверка выделенного хоста/учётки, отдельный каталог/Compose/network/volumes/secrets/backups, прокси-маршрут, backup/restore rehearsal, создание реальных учёток и ручная ПК/мобильная проверка. Старый ODH-контур и его БД/секреты не использовались.

## 2026-09-15 — production rollout preparation

- После разрешения пользователя на production проверен выделенный хост `obhod-sao.ru` по SSH: отвечает `PREFASAO`; старые контейнеры `odh-sao-*`, `jirajura-*` и их сети оставлены без изменений.
- Добавлены отдельные `sao-photo-service-edge`/nginx-конфигурация, backup/restore/monitor shell-команды, cron-шаблон и CI workflow. Proxy будет подключаться к edge-сети отдельно; PostgreSQL/media остаются в приватных сетях/томах новой службы.
- Полный локальный integration smoke на одноразовых томах: migration + import 11 275 объектов, индивидуальная prefecture account, `gps_required` без GPS, upload 201, idempotent повтор 200, review confirmed 200, summary 11 268 assigned + 7 unassigned, XLSX 410 731 bytes, PDF 1 933 bytes. Временные ресурсы удалены.
- `npm test` — 34/34; `npm audit --audit-level=high --omit=dev` не блокируется critical/high, но остаётся два moderate advisory в ExcelJS/uuid metadata; установлен uuid 11.1.0 override, breaking downgrade не применялся. Этот риск помечен для post-deploy security review.
- Перед production остались только операционные действия: передача версии на хост, создание отдельной `.env`/секрета и backup-каталога, миграция/import, proxy include с конфигурационным backup, smoke/rollback и ручное создание согласованных индивидуальных аккаунтов. Секреты в репозитории и логи не добавлять.

## 2026-09-15 — production rollout completed for service edge

- Версия `0121096` передана в отдельный каталог `/opt/sao-photo-service`; production `.env` создан с режимом `600`, секреты в Git и чате не раскрывались.
- Поднят отдельный Compose-проект `sao-photo-service`: PostgreSQL и media volumes принадлежат только ему; приложение здорово, `/healthz` возвращает `database=connected`. Импорт применён: 11 275 report objects (812 stop, 428 PP, 10 035 entrance), 7 объектов остаются unassigned по утверждённой модели.
- Перед proxy-изменением создан и проверен backup `/var/backups/sao-photo-service/20260915T055418Z` (database dump и media archive, `sha256sum -c` успешно).
- Существующий Nginx proxy получил только новый read-only include/mount и подключение к внешней `sao-photo-service-edge`; старая `jirajura_default`, старые сервисы, БД, тома и секреты не переиспользовались. `nginx -t` успешен.
- Публичный smoke успешен: `https://obhod-sao.ru/photo-api/healthz` → HTTP 200; unauthenticated `/photo-api/auth/me` → HTTP 401. Старый `https://obhod-sao.ru/odh-api/api/health` → HTTP 200.
- Сохранены rollback-копии `/opt/jirajura/docker-compose.prod.yml.pre-sao-photo-20260915T055433Z` и `/opt/jirajura/deploy/nginx/active.conf.template.pre-sao-photo-20260915T055433Z`.
- Реальные индивидуальные учётки ещё не заведены: их создаёт администратор на сервере через скрытый ввод пароля после проверки списка районов. Пока аккаунты не созданы, отправка фото пользователями не начнётся.
- Установлен отдельный `/etc/cron.d/sao-photo-service` (ежедневный backup в 02:15 UTC и monitor каждые 5 минут); ручной monitor прошёл, свободно 44% media.
- Production SQL-проверка подтвердила `entrance=10035`, `pp=428`, `stop=812`, `users=0`, `photos=0`; пустые users/photos ожидаемы до создания районных учёток и первой фиксации.
- Коммит `a2a4a34` fast-forward отправлен в `origin/main`; Pages, SAO photo-service quality и SMM/atlas regression workflows завершились `success`. Публичные три карты после cache-bust отдают `PHOTO_API_BASE`.

## 2026-09-15 — районный логин без email-ограничения

- Пользователь изменил схему доступа: одна общая `district_editor`-учётка на район, логин — название района, всего 16 районов. Предыдущая запись об индивидуальных учётках сохранена как историческое решение.
- API теперь принимает `login` и сохраняет обратную совместимость с JSON `email`; CLI `create-user.js` принимает `--login` (алиас `--email`). Карты используют обычное текстовое поле «Название района», чтобы кириллица не блокировалась HTML-валидатором `type=email`.
- Парольная политика не ослаблена: минимум 12 символов. `SaoFoto1`…`SaoFoto16` короче порога, поэтому production users не создавались и ждут 12+ варианта.

## 2026-09-15 — отдельная страница атласа, изоляция клиента и найденные дефекты

Пользователь попросил перевести три карты объектов на отдельную страницу атласа и выбрать, как поступить с сессией при кросс-доменном размещении. Решения пользователя: одна страница со всеми тремя наборами (старые ссылки продолжают работать) и cookie `SameSite=None` с запасным bearer-токеном. Отдельно согласовано встраивать в Excel миниатюру 320 px вместо оригинала.

### Страница атласа и клиент

- Данные трёх карт вынесены из встроенных `<script id="map-data">` в `object-maps/data/{stops,pp,entrances}.json`; манифест `object-maps/data/manifest.json` фиксирует версию источника, дату и SHA-256 каждого набора. Число записей не изменилось: 812 / 2 235 / 10 035.
- Добавлена страница `object-maps/`: переключатель наборов, режим «Ведомость по району» для ПК и «Маршрутная очередь» для телефона. `stops.html`, `pp.html`, `entrances.html` стали тонкими оболочками на том же клиенте, поэтому старые адреса работают.
- Общий клиент вынесен в `object-maps/photo-client.js` + `photo-client.css`, чистая логика — в `photo-model.js`. Три копии по ~700 строк заменены одним модулем.
- В атласе три карточки фотофиксации заменены одной, ведущей на новую страницу.
- Карточка объекта открывается нативным `<dialog>`: ловушка фокуса и возврат фокуса обеспечиваются платформой, добавлен один видимый стиль `:focus-visible` для всех элементов.

### Найденные и исправленные дефекты

Все найдены при проверке, а не по жалобам, и ни один не был в прежнем чекпойнте.

1. **Сессия не работала в браузере вообще.** Карты живут на `hainox.github.io`, служба — на `obhod-sao.ru`; cookie `SameSite=Lax` не отправляется в кросс-сайтовом запросе. Добавлено настраиваемое `PHOTO_SERVICE_COOKIE_SAMESITE` (production — `none`, что принудительно включает `Secure`) и запасной `Authorization: Bearer` для браузеров, блокирующих сторонние cookie. Токен в `sessionStorage` — осознанный компромисс, согласованный с пользователем.
2. **Разбор полей фото в клиенте был сломан.** `GET /photos` отдаёт snake_case, клиент читал camelCase: дата показывалась как `Invalid Date`, точность GPS и дистанция не показывались вовсе, статус проверки молча терялся. Нормализация перенесена в `photo-model.js` и покрыта тестами на обе формы ответа.
3. **Повтор отправки создавал дубль.** Клиент генерировал новый `Idempotency-Key` на каждое нажатие. Теперь ключ стабилен для пары «объект + файл» и сбрасывается только после успеха. Проверено сценарием «ответ потерян»: вторая отправка возвращает сохранённую фиксацию, в базе остаётся одно фото.
4. **Сводка читалась не оттуда.** Проценты и полоса лежат в `overall`, клиент искал их в корне ответа и падал на `toFixed` при входе. Исправлено, форма ответа закреплена тестом.
5. **Роль района видела чужие объекты.** Сервер ограничивал выдачу, но клиент фильтровал список по пустому району и показывал все 812 остановок вместо своих 952. Фильтр по району учётки добавлен в клиент.
6. **Выдача фото и обе выгрузки не отдавали CORS.** Клиент читает байты фото авторизованным запросом, поэтому ответу нужны те же заголовки, что и у JSON. Excel и PDF не открывались с домена атласа вовсе.
7. **Ограничение входа считало успешные попытки.** Районный офис сидит за одним внешним адресом, и десять обычных входов за 15 минут блокировали всех. Счётчик теперь считает только неудачи; логика вынесена в `src/login-throttle.js` и покрыта тестами. Дефект нашёлся именно потому, что повторные прогоны e2e начали упираться в 429.

### Отчёты и происхождение данных

- Миграция `002_import_runs.sql`: каждый применённый импорт записывается с версией источника, хешами наборов, количеством объектов и **перечнем** объектов без района. Dry-run перечисляет все 7 нераспределённых объектов с номерами исходных строк — 6 остановок (37, 556, 557, 583, 586, 688) и один `odh_id` ПП (173, 174); совпадает с `dev/Data-Audit.md`.
- Импорт читает JSON-наборы и сверяет SHA-256 с манифестом; при расхождении останавливается вместо тихой загрузки другого набора.
- Отчёты печатают дату формирования и версию набора объектов.

### Выгрузка Excel: измеренная проблема

- Полный экспорт САО с полноразмерными оригиналами: 11 701 фото по 80 КБ дают книгу **872 МБ** и пик памяти **3,9 ГБ** — это уронило бы общий хост при нажатии «Excel» префектурой.
- Миграция `003_photo_thumbnail.sql` и необязательный кадр `thumbnail` (320 px, до 512 КБ, JPEG): клиент делает копию при отправке, сервер хранит её рядом с оригиналом, Excel встраивает только её.
- После правки: полный САО — 130 МБ, 13 с, пик 1,1 ГБ; районный объём (730 объектов, 2 фото) — 16 МБ, 1,5 с. Оригиналы остаются на сервере и открываются в карточке.
- Инструмент `scripts/excel-load-test.js` воспроизводит замер; зафиксирован в README вместе с числами.

### Проверки

- `npm test` в `photo-service` — 49/49; `npm test` в `object-maps` — 15/15 (модель клиента).
- Изолированный стенд: отдельный Compose project, миграции 001–003, импорт 11 275 объектов, две тестовые учётки. Браузерная проверка `odh-map/tests/photo-atlas.e2e.mjs` — 44 проверки, три прогона подряд без падений, на видах 1280×800 и 390×844.
- `npm audit --omit=dev` — 0 уязвимостей. Override `uuid` поднят с 11.1.0 до 11.1.1 (исправленная версия из GHSA-w5hq-g745-h8pq); breaking `audit fix --force` по-прежнему не применялся.
- Замеры Excel сняты на синтетических изображениях; реальные фотографии того же размера дадут тот же порядок величины.

### Осталось

- Ручная проверка глазами по `dev/MANUAL-CHECK-photo-atlas.md`: автоматика не заменяет взгляд на экран, особенно на реальном телефоне.
- Открыт вопрос знаменателя: нераспределённые объекты показываются отдельной строкой и в знаменатель САО не входят.

## 2026-09-15 — production rollout новой страницы атласа

- Коммит `3da4382` fast-forward отправлен в `origin/main` (`12b452d..3da4382`). Workflows по этому коммиту завершились `success`: «SAO photo service quality» (run 3), «SMM overlay & atlas checks» (run 27), «pages build and deployment» (run 101).
- Перед изменениями на хосте создана и проверена резервная копия `/var/backups/sao-photo-service/20260915T065243Z`: `database.dump`, `env.backup` (режим 600) и `code-before.tar.gz`; `sha256sum -c` успешен.
- Версия доставлена архивом из закоммиченного состояния (`git archive HEAD`) в `/opt/sao-photo-service`. Хост не является git-чекаутом; `.env` в архив не входит и не перезаписывался.
- В production `.env` добавлено `PHOTO_SERVICE_COOKIE_SAMESITE=none`; остальные ключи не менялись, секреты не выводились.
- Применены миграции `002_import_runs.sql` и `003_photo_thumbnail.sql`, образ пересобран, служба пересоздана. Импорт применён повторно: хеши наборов совпали с локальными, 812 stop / 428 pp / 10 035 entrance, 7 объектов без района; в `import_runs` записан прогон с версией и перечнем.
- Созданы 16 районных учёток; пароли вводились скрыто через stdin и не попадали в аргументы команд. Логины — нормализованные названия районов; посторонние контейнеры (`jirajura-*`, `odh-sao-*`) не затронуты. Пароли в репозитории не хранятся.
- Публичный smoke: `https://obhod-sao.ru/photo-api/healthz` → HTTP 200; вход возвращает `Set-Cookie … SameSite=None; Secure; Path=/photo-api` и CORS-заголовки для `https://hainox.github.io`; `GET /auth/me` без сессии → 401; Excel-выгрузка с сессией → 200, корректный `Content-Type`, CORS, файл начинается с `PK`; сводка района вернула 952 объекта для «Аэропорт»; старый `https://obhod-sao.ru/odh-api/api/health` → HTTP 200 и не изменён.
- Живая проверка опубликованной страницы против production API (`odh-map/tests/photo-atlas-live.mjs`, только чтение, без создания фиксаций): 13 из 13 — страница открывается, видны три набора, **вход с домена атласа сохраняет сессию**, сводка района 952 объекта с версией набора, реестр ограничен своим районом, карточка и галерея открываются, Esc закрывает, переключение набора и маршрутная очередь работают, ошибок страницы нет.
- Выкат подтверждает, что кросс-доменная сессия починена: до этой правки вход с домена атласа не работал вовсе.

### Осталось после выката

- Ручная проверка глазами на ПК 1280×800 и реальном телефоне 390×844 по `dev/MANUAL-CHECK-photo-atlas.md`.
- Первая реальная фиксация: проверить путь фото целиком, включая миниатюру и её попадание в Excel.
- Открыт вопрос знаменателя по нераспределённым объектам.

## 2026-09-15 — переименование набора подъездов

- Отображаемое название набора изменено с «Подъезды InJob» на «Подъезды (Входная группа)»: в самом наборе `object-maps/data/entrances.json` и в манифесте.
- Переименование не тронуло данные: 10 035 записей и 10 000 групп экспорта остались прежними, изменилась одна строка заголовка. Побайтовая замена, а не пересериализация JSON.
- Манифест пересчитан: новый SHA-256 файла набора. Импорт по-прежнему проходит проверку хеша и даёт те же контрольные числа — 812 / 2 235 / 10 035 и 7 объектов без района.
- Внутренние идентификаторы намеренно не менялись: `datasetId` остаётся `injob_entrances`, потому что это ключ связи с уже загруженными объектами в production.
- `dev/Data-Audit.md` не правился: это датированный снимок, и упоминания `InJob (30).xlsx` там описывают реальный исходный файл, а не подпись в интерфейсе.

## 2026-09-15 — разграничение доступа районов, границы на карте и мобильная раскладка

Решения пользователя: районные учётки только снимают фото и не видят выгрузок вообще (ни Excel, ни PDF, ни CSV, ни скачивания отдельного снимка); учётка префектуры — верхний уровень с логином «Префектура».

### Доступ

- `POST /photos` остаётся единственным действием района. Выгрузки `export.xlsx` и `export.pdf` теперь требуют роль префектуры и отвечают `403 prefecture_role_required` — проверка серверная, а не только скрытая кнопка. Проверка вынесена в общий `requirePrefecture` и применена также к review и delete.
- В клиенте блок выгрузок целиком скрыт для района: Excel, PDF и CSV. Ссылка «Скачать фото» тоже не создаётся — район видит снимок в галерее, но не выгружает файл.
- Район ограничен своим районом и на клиенте: список, очередь и метки на карте фильтруются по району учётки, объекты других районов в интерфейс не попадают, а серверная выборка остаётся главным ограничением.
- **Честная граница меры:** файлы наборов `object-maps/data/*.json` лежат в публичном атласе, поэтому адреса, координаты и атрибуты объектов другого района технически доступны любому, кто откроет файл. Разграничение закрывает интерфейс и все данные о фотофиксации, но не делает справочник объектов секретным. Чтобы закрыть и его, справочник нужно переносить за API — это отдельное решение.

### Границы районов

- Полигоны берутся из `districts.geojson` (16 районов) и рисуются на карте: у района — только его граница, у префектуры — все 16, а при выборе района в фильтре остаётся одна.
- GeoJSON хранит `[долгота, широта]`, Яндекс Карты ждут `[широта, долгота]` — перестановка вынесена в чистую `districtBoundaries` и покрыта тестами.
- Контур не перехватывает клики по меткам (`interactivityModel: 'default#silent'`).
- Что именно показано, сказано словами: «Показана граница района: Аэропорт.» или «Показаны границы всех 16 районов.»

### Мобильная раскладка

- Пользователь сообщил, что на телефоне до карты приходилось проматывать весь список. Так и было: в одноколоночной раскладке сайдбар шёл первым в потоке.
- Теперь на узких экранах карта занимает рабочую область целиком, а ведомость (сводка, фильтры, список) открывается отдельной кнопкой «Список и фильтры» как нижний ящик поверх карты, с подложкой, кнопкой «Закрыть» и закрытием по Esc. Выбор объекта и переключение режима закрывают ящик.
- Самопроверкой подтверждено, что карта видна без прокрутки, высота страницы не превышает экран, а кнопка открывает и закрывает список.

### GPS: расстояние до объекта

- Пользователь попросил показывать, на каком расстоянии от выбранного объекта находится сотрудник.
- После получения координат клиент показывает «До объекта 12,4 м — в радиусе 15 м.» или «риск: дальше 20 м» и предупреждает, что фиксация уйдёт с пометкой риска.
- Расчёт повторяет серверный: та же формула гаверсинуса, тот же номинальный радиус 15 м и допуск 5 м, и то же правило «ближайшая зарегистрированная точка». Для ПП это важно: отчётный объект объединяет несколько координатных строк, и расстояние считается до ближайшей из них. Точки запрашиваются через `/objects/resolve` и кешируются; без авторизации используется точка самой строки.
- Совпадение с сервером закреплено тестом: клиентская и серверная реализации сверяются на одном наборе координат.

### Найденные при этом дефекты

1. **Регрессия в разграничении района.** При объединении функций `selectedDistrict` я инвертировал условие, и роль района снова стала видеть все 812 объектов вместо своих 952. Поймано браузерной проверкой. Разделено на две явные функции: `scopeDistrict` (что показываем) и `requestedDistrict` (что просим у сервера).
2. **`hidden` не работал из-за авторского `display`.** В CSS у `.pa-exports`, `.pa-login` и `.pa-queue` задан явный `display`, который перебивает браузерное правило для атрибута `hidden`. Из-за этого блок выгрузок, форма входа и панель очереди оставались видимыми всегда. Добавлено одно правило `[hidden] { display: none !important; }` и проверки на это.

### Проверки

- `npm test` в `photo-service` — 49/49; `npm test` в `object-maps` — 26/26.
- Браузерная проверка `odh-map/tests/photo-atlas.e2e.mjs` — **68 проверок, три прогона подряд без падений**, включая отказ района в выгрузках на уровне сервера, скрытие выгрузок в интерфейсе, границу района на карте, мобильную раскладку с ящиком и расстояние в сообщении GPS.
- Регрессия: `python -m pytest tests -q` — 26, `npm run test:ui` из `odh-map` — 18, `npm test` из `api` — 15.

### Осталось

- Учётка префектуры создаётся на production в этом же проходе; логин «Префектура».
- Ручная проверка глазами по `dev/MANUAL-CHECK-photo-atlas.md`, особенно мобильной раскладки на реальном телефоне.

## 2026-09-15 — границы районов больше не проходят по водохранилищу

- Пользователь сообщил, что граница района нарисована по акватории Химкинского водохранилища. Это оказалось не ошибкой отрисовки: `districts.geojson` собран из выгрузки муниципальных образований Москвы (GIS-Lab, границы OSM/ODbL), и тамошняя граница действительно включала воду.
- Проверил это, а не предположил: точки на акватории попадали внутрь полигонов (центр водохранилища и западный плёс — в Левобережном, южная часть — в Головинском). Точность самих полигонов подтверждена независимыми данными — 2 292 двора из `areas.geojson` (99,9 %) попадают в свой район, что исключает сдвиг или путаницу координат.
- Контур акватории получен из OSM (Nominatim, relation хранится как `water.geojson`, 652 точки, ODbL) и вырезан из полигонов скриптом `work/apply_water_mask.py` через shapely — она уже объявлена в `requirements-dev.txt`.
- Затронуты три района: Войковский (7,7 % площади), Головинский (2,5 %) и Левобережный (14,5 %). Левобережный после вырезания стал `MultiPolygon` из двух частей — клиент научен рисовать несколько частей и дырки, на это добавлены тесты.
- **Вырезание не изменило районное назначение объектов**: импорт до и после даёт одинаковые контрольные числа — 812 остановок, 428 отчётных ПП, 10 035 подъездов и те же 7 объектов без района. Это проверялось именно так, потому что `districts.geojson` используется ещё и для point-in-polygon при импорте.
- Акватория вырезана только для Химкинского водохранилища — это единственный крупный водный объект на карте. Мелкие реки и пруды не трогали: если что-то из них тоже пересекает границу, добавить их можно тем же скриптом, дописав контур в `water.geojson`.
- Проверки: `npm test` в `object-maps` — 28/28 (добавлены случаи MultiPolygon и дырки), `python -m pytest tests -q` — 26, браузерная проверка — 69 проверок без падений, включая район, разрезанный водой.

## 2026-09-15 — читаемый PDF, коды в отчётах и отзыв утёкших паролей

### PDF не читался

- Пользователь прислал содержимое PDF-отчёта. Отчёт печатался встроенным в PDFKit шрифтом Helvetica: он не умеет кириллицу, текст писался в WinAnsi без таблицы `ToUnicode`. Из-за этого отчёт и выглядел неверно, и при копировании давал ту самую абракадабру.
- Воспроизвёл у себя: сгенерированный образец побайтово совпал с тем, что прислал пользователь, и извлёкся через pypdf так же нечитаемо. Причина подтверждена, а не предположена.
- В комплект добавлен `assets/fonts/PT_Sans-Web-Regular.ttf` (PT Sans, ParaType, OFL; рядом `OFL.txt` и `METADATA.pb`), Dockerfile копирует `assets`. После правки в PDF стоит `Type0`/`Identity-H`, встроенный `FontFile2` и есть `/ToUnicode`, а извлечённый текст читается по-русски.
- Тест закрепляет механизм: в PDF должны быть `FontFile2` и `ToUnicode` и не должно быть `Helvetica`.

### Коды вместо слов

- Отчёт печатал «Статус: low» и «stop: 0/806» — внутренние коды. Теперь в PDF и в обеих книгах Excel статусы идут словами («Красный», «Жёлтый», «Зелёный»), типы объектов — «Остановки», «ПП», «Подъезды», а проценты округлены до десятых.
- Заодно из PDF убраны длинные тире: их нет в выбранном шрифте, а невидимый символ в отчёте выглядел бы как пропуск.

### Пароли попали в публичный репозиторий

- **Инцидент собственный:** я записал рабочие пароли всех 17 production-учёток в `dev/CHECKPOINT` и `dev/MANUAL-CHECK`, а репозиторий публичный. Пароли были видны всем.
- Из отслеживаемых файлов пароли убраны, в документации осталось только место хранения — файл учёток у администратора. Историю коммитов переписывать не стал: репозиторий публичный, а переписывание чужой истории опаснее, чем замена паролей.
- **Все 17 паролей заменены на случайные** (16 символов в четырёх группах, алфавит без похожих символов). Скрипт `scripts/rotate-accounts.js` задаёт новые пароли, отзывает активные сессии, проверяет вход через публичный API каждого аккаунта и складывает таблицу в xlsx; в консоль пароли не печатает.
- Результат: заменено 17, вход проверен 17 из 17. Файл учёток лежит у пользователя на рабочем столе, на сервере временная копия удалена.
- Для одиночной замены добавлен `scripts/set-password.js` со скрытым вводом, он тоже отзывает сессии учётки.
- Вывод на будущее: в этом репозитории нельзя писать в файлы ничего, что является рабочим секретом, даже в документации.

### Ошибка в собственном скрипте

- Первые прогоны `rotate-accounts.js` молча не писали файл: функция разбора аргументов ожидала имя без дефисов, а вызывалась как `option('--out')`. Пароли при этом менялись, а выгрузки не было. Нашлось при проверке результата, исправлено на `argument('out')` с явной ошибкой при отсутствии значения.

### Проверки

- `npm test` в `photo-service` — 52/52 (добавлен `test/exports.test.js`).
- Production: публичная выгрузка PDF извлечена и прочитана — «Статус: Красный», «Остановки: 0 из 806», «0,0 %»; шрифт `PTSans-Regular`, `Type0`/`Identity-H`, `ToUnicode`.

## 2026-09-15 — графики, диаграммы и динамика в отчётах

- Пользователь попросил добавить в отчёты графики, диаграммы и показатели динамики, сделать их нагляднее и презентабельнее.
- Данные для визуализации считаются отдельно от отрисовки: `summarizeByDistrict`, `uploadDynamics` и `completionMix` в `src/report.js` — чистые функции, покрытые тестами. Рисование PDF — в `src/pdf-charts.js`, подписи отчётов вынесены в `src/labels.js`, чтобы PDF и Excel говорили одинаковыми словами.
- PDF стал двухстраничной сводкой с диаграммами: крупный процент со шкалой и значком статуса, составная полоса состояния объектов, полосы по типам, столбцы загрузки по дням за 14 дней и, когда районов больше одного, страница с полосами по районам. Диаграммы векторные, без новых зависимостей.
- Excel получил листы «Районы» и «Динамика», а ключевые числа — полосы в ячейках через родное условное форматирование `dataBar`. Это не картинки: полосы масштабируются, печатаются и читаются любой версией Excel.
- Новые агрегаты проверены тестами: динамика по дням в UTC с накопленным итогом, включая фотографии старше окна; разрез по районам; состав состояний без двойного счёта. Тест сразу поймал дефект — «Без района» вставал в середину списка вместо последней строки, потому что пустая строка сортировалась раньше букв. Исправлено.
- Нагрузочная проверка на полном объёме не изменилась: 11 701 объект с миниатюрами — 130 МБ Excel за 9,1 с при пике 1,06 ГБ; PDF 28 КБ за 0,1 с.
- `npm test` в `photo-service` — 57/57.

## 2026-09-15 — дашборд округа для префектуры и корпоративный шрифт

### Дашборд

- Пользователь попросил для префектуры небольшой дашборд в боковой панели с главной информацией по округу и районам.
- Разрез по районам теперь считается на сервере и приходит в `/reports/summary` полем `byDistrict` — это те же числа, что в PDF и Excel, а не вторая независимая реализация. Экспорты переведены на это же поле.
- В боковой панели у префектуры появилась доска «Районы округа»: по каждому району доля завершённых объектов, полоса по цвету статуса и процент. «Без района» идёт последней строкой, отдельным оформлением и без перехода. Клик по району фильтрует всю панель.
- Когда префектура проваливается в один район, доска не исчезает: показывается сохранённый срез по всему округу. У районной учётки доски нет — ей и так виден только свой район.
- Проверки: `npm test` в `photo-service` — 60/60 (добавлен `test/reports.test.js`), браузерная проверка — **77 проверок без падений**, включая 17 строк доски, порядок «Без района» и фильтрацию по клику.

### Шрифт

- Пользователь сообщил, что корпоративный шрифт — Century Gothic. В PDF тогда стоял PT Sans.
- Положить Century Gothic в комплект нельзя: шрифт коммерческий, а репозиторий публичный. Поэтому:
  - в комплект добавлен `assets/fonts/Jost-Regular.ttf` — свободный геометрический шрифт (SIL OFL), ближайший бесплатный аналог по рисунку;
  - добавлена переменная `PHOTO_SERVICE_PDF_FONT`: лицензионный файл ставится на сервер и подключается без попадания в репозиторий; при отсутствии файла печатается предупреждение и берётся встроенный шрифт;
  - интерфейс карт переведён на стек с Century Gothic первым — на машинах, где он установлен, страница выглядит корпоративно.
- При выборе пришлось разбираться: PDFKit берёт из вариативного TTF базовый инстанс, и Montserrat встраивался как **Thin**, Raleway тоже Thin, Manrope — ExtraLight. Для отчёта это неприемлемо. Проверил сборкой: Regular дают Jost, Comfortaa и Golos Text; выбран Jost — ближе всех к Century Gothic и вдвое легче остальных. Единственный пропущенный символ — `№`, в подписях отчёта он не используется.
- Тест на PDF держался на пороге размера файла как косвенном признаке встроенного шрифта; при более лёгком шрифте порог стал ложным. Убрал его: настоящий признак — наличие `FontFile2` и `ToUnicode`, они и проверяются.

### Проверки

- `npm test` в `photo-service` — 60/60; браузерная проверка — 77 из 77.
- PDF на образце: две страницы, встроенный `Jost-Regular`, `Type0`/`Identity-H`, `ToUnicode`, текст извлекается по-русски.

## 2026-09-15 — аудит всех карт хаба, права района и выкатка оповещений

### Аудит карт

- Пользователь попросил провести аудит всех карт в хабе, найти и исправить ошибки и выяснить, начали ли районы грузить фото.
- Проверены все 18 страниц (12 карточек хаба и внутренние контуры) на двух разрешениях: 1440 и 390 px.
- Локальные наборы без замечаний: playwright 19/19, pytest 26/26, логика клиента фотофиксации 28/28, статические валидаторы HUD, районных правок, эталонной печати и хаба.
- Живой прогон: у всех карт на Leaflet плитки загружаются полностью (24/24, 118/118, 104/104), карты на Яндекс JS API строятся (canvas и метки), битых ссылок на ресурсы нет.
- Ложная тревога первого прогона: карта СММ создаётся только при переключении на вкладку «Карта», поэтому проверка «canvas на первом экране» к ней не подходит. Отдельная проверка переключением вкладки: canvas 1026×420, 6 меток, ошибок нет.
- Реальный дефект один: памятка префектуры на узком экране давала горизонтальную прокрутку 20 px — таблицы шире экрана. Исправлено обёрткой `.table-scroll` с прокруткой внутри блока; сама страница больше не скроллится вбок. Правка сделана только для узких экранов, печать и широкий экран не затронуты.
- Репозиторий и GitHub Pages сверены побайтово: расхождений по содержимому нет, отличаются только переводы строк (CRLF в рабочей копии против LF в раздаче GitHub).

### Права района на проде

- Проверены все 16 районных учёток: каждая видит только свой район, чужих объектов не видно, выгрузки Excel и PDF закрыты (403), фото чужого объекта — 404. Провалов нет.

### Фактические загрузки фото

- На момент проверки: 392 объекта с фото из 11 268, все со статусом «ждёт приёмки», подтверждённых нет, 4 объекта с геориском.
- Работают три района: Восточное Дегунино — 165, Ховрино — 152, Аэропорт — 74; Западное Дегунино — 1 объект. Остальные 13 районов не начинали.
- За час между двумя замерами добавилось 113 фотографий: районы грузят прямо сейчас.

### Выкатка оповещений

- На хосте код оповещений уже лежал, но им никто не пользовался: фотослужба была собрана из старой копии без `src/notify.js`, API работал без `notifier`, контейнер `notify` не запускался.
- Заново собраны и подняты: `sao-photo-service-photo-service-1` (в контейнере есть `src/notify.js`, заданы `NOTIFY_URL` и `NOTIFY_SECRET`), `odh-sao-api-1` (в логе «оповещения включены»), `odh-sao-notify-1`.
- Хеши развёрнутых исходников совпадают с репозиторием; публичные адреса отвечают 200.
- Ограничение среды: сам хост не достаёт `api.telegram.org`. DNS отдаёт и A-, и AAAA-запись, но исходящий IPv6 не работает, а соединение по IPv4-адресу из DNS не устанавливается; на адрес, указанный вручную, Telegram отвечает. Бот запущен и здоров (`/health`), но сообщения до Telegram не доходят — нужен прокси или релей. Пользователь просил бота сейчас не дорабатывать.
- Резервные копии перед выкаткой: `/var/backups/sao-deploy/photo-service-before-notify.tar.gz` и `odh-sao-before-notify.tar.gz`.

### Проверки

- Публичные адреса: `/photo-api/healthz` — 200, `/odh-api/api/health` — 200.
- Фотослужба после пересборки: вход префектуры 200, сводка 200.
- ODH API: неверный пароль — 401, запрос без токена — 401 (маршруты и защита на месте).
- Атлас: правка памятки доехала до GitHub Pages.

## 2026-09-15 — сводная отчётность фотофиксации и автоматический риск

### Что сделано

- Выгрузка фотофиксации переделана в единую сводную отчётность. Лист «Сводка» заменён на «Обзор»: титул со снимком даты и версией набора, блок «Охват» (объектов, с фото, без фото, охват, выполнено, на проверке) и блок «Риск» по категориям. Лист «Районы» получил охват, число рисков и строку ИТОГО. Лист «Риск» добавлен отдельным пунктом: дата выявления, категория, район, балансодержатель, объект, ID объекта ОДХ, координаты, исполнитель, координаты и точность GPS, расстояние до объекта, превышение зоны, превью фотографии, статус. У всех листов единое оформление: шапка с заливкой, зебра, закреплённые строки, автофильтры, полосы данных на процентах.
- Добавлен модуль `photo-service/src/risks.js`: автоматическое выявление рисков. Статус присваивается по факту попадания в категорию, ручного разбора нет. Категории: превышение зоны (дальше 20 м — номинал 15 плюс допуск 5) и точный дубль файла по `sha256`. Дубль на одном объекте риском не считается: это обычная повторная съёмка.
- В выборку отчёта добавлены производные колонки: балансодержатель (`Балансодержатель` или `Баланс` из свойств) и ID объекта ОДХ; в JSON фотографии добавлен `sha256`, без которого дубли не поймать.

### Решения

- Категорию «Отклонено приёмкой» в риски не включаем: по указанию заказчика это решение префектуры, а не нарушение района.
- Критичность единая, сроков и просрочки нет — поэтому в записи риска нет ни градаций, ни дедлайнов, а в лист не добавлены соответствующие колонки.
- У подъездов своего балансодержателя в источнике нет: подставляется «Жилищник «Район»» — так же, как это поле названо в редакторе правок ОДХ.
- Выгрузка заменена, а не продублирована: второй формат не создавался.

### Проверки

- Тесты фотослужбы: 67/67, включая восемь новых — на выявление рисков и на отдельный лист «Риск» в книге.
- Питоновский набор репозитория: 27/27. Тесты API карты ОДХ: 18/18.
- Попутно обнаружено, что в службе не были установлены `exceljs` и `busboy`: два файла тестов не запускались вообще. Зависимости установлены, прогон стал полным.

### Остаток

- Прод-служба фотофиксации не пересобрана: изменения живут только в репозитории.
- Данные на 15.09: 442 фото, 435 объектов, 12 исполнителей, 5 районов из 16. Рисков по новым правилам: превышение зоны — 4, точный дубль — 1 пара (два подъезда одного дома, разница загрузок 18 секунд).
- В рабочей базе пять тестовых записей («Тест», «Тест 2», «Тест 3», «Тест_ПП») и исполнитель под двумя написаниями (Керимов Р.Ш. / Керимов РШ) — искажают сводку, требуется чистка.
- Точность GPS хуже 5 м у 433 фото из 442: снимки нельзя надёжно привязать к объекту. Решение о том, считать ли это риском, за заказчиком.

### Ошибка и исправление

- В первом прогоне два теста рисков падали: вспомогательная функция в тесте выдавала всем фото одинаковый `sha256`, и модуль справедливо находил дубли там, где их не закладывали. Исправлено в тесте, не в модуле.

## 2026-09-15 — маршруты ДКМ и ТУ, права префектуры, снятие слоя СММ

### Что сделано

- В словарь типов разметки добавлены маршруты ДКМ и ТУ: существующие уточнены припиской «— ОДХ», добавлены дворовые варианты «— дворы». Все четыре работают как маршруты: начало и конец, направление движения, сопло; дворовые отличаются пунктиром. Словарь продублирован в трёх местах (фронтенд, валидация API, список маршрутов на карте) — правка внесена во все три.
- Отдельная роль приёмки `reviewer` упразднена: приёмка, обе выгрузки и фото-метки доступны только роли префектуры. Миграция 003 переводит существующие записи `reviewer` в `prefecture_admin` и сужает CHECK-ограничение.
- Общий список наборов закрыт для района в любом виде, включая параметр запроса. Добавлена ручка `/api/my-submissions`: район видит только статус своих наборов и только своего района. В редакторе района появилась панель «Мои отправки» с кнопкой запроса статуса; после успешной отправки список обновляется сам.
- В карточке набора поле района переименовано в «Балансодержатель», подписи вариантов — «Жилищник «Район»».
- Со всех рабочих карт ОДХ и с корневой карты дворов добавлен возврат в городской атлас; печатные формы намеренно не тронуты.
- С корневой карты удалён слой «Маршруты СММ»: слой, панель быстрых переходов, маркеры направлений, загрузка `smm_routes.geojson`, вспомогательные функции расчёта азимута и раскладки стрелок, их SVG и CSS, запись из манифеста. Файлы `smm.geojson` и `smm_routes.geojson` оставлены на месте: это исходные данные секции `smm/`, из которых собираются оверлей маршрутов и печатная форма.

### Решения

- Значение опции балансодержателя оставлено именем района: подпись меняется, значение нет. Оно уходит в наборы, валидацию, базу и привязку учётных записей.
- Новую роль для ДКМ и ТУ не заводили: ДКМ и ТУ заполняют районы, то есть работают в существующем районном контуре. Районный замок не снимался.
- Слой `smm_storage` («Места хранения СММ») не тронут — это объекты ОДХ, а не маршруты.
- Страницы `smm/` и карточка раздела в хабе оставлены: удалялся слой с корневой карты.

### Проверки

- Тесты API: 18/18, включая отказ району на приёмку и выгрузки, доступ к своим статусам и отказ упразднённой роли.
- Питоновский набор тестов репозитория, `python -m pytest tests` — 27/27: секция СММ, генераторы, корневая карта.
- Синтаксис всех изменённых страниц проверен компиляцией встроенных скриптов.
- Удаление слоя выполнено скриптом со сверкой каждой удаляемой строки; мёртвых ссылок на удалённые функции не осталось.
- Живой прогон корневой карты после выкатки: список слоёв без «Маршруты СММ», объектов 7217 против 7222 до правки (ровно минус 5 объектов слоя), ошибок консоли и битых запросов нет. Локальная проверка невозможна: ключ Яндекс-API привязан к домену и с localhost блокируется.

### Ошибка и исправление

- Вместе со слоем были удалены файлы `smm.geojson` и `smm_routes.geojson` как «данные удалённого слоя». Это неверная оценка: `smm.geojson` — исходник секции `smm/`, из него `work/build_smm_routes_overlay.py` собирает оверлей маршрутов, а `work/build_smm_print_a3.py` — печатную форму; на них завязаны четыре скрипта в `work/` и workflow `smm-atlas.yml`.
- Причина, по которой это не поймали до пуша: прогонялись тесты API и проверка синтаксиса страниц, но ни разу не запускался питоновский набор тестов репозитория (`python -m pytest tests`), который покрывает и секцию СММ, и эти файлы. CI показал 11 падений.
- Исправление: файлы восстановлены; два теста корневой карты переписаны под новое поведение — теперь они стерегут, что слой СММ не вернётся, а полнота данных секции проверяется отдельным тестом; из теста генератора убраны проверки отрисовки на корневой карте, контракт данных сохранён.
- Вывод на будущее: перед пушем запускать полный набор тестов репозитория, а не только тот, что кажется относящимся к правке.

### Остаток

- Прод-API не пересобран: на живой карте типы ДКМ/ТУ уже есть, а API их отклонит, пока контейнер на сервере не обновлён.
- Подсветка ОДХ с переключателями в редакторе района не сделана; вес слоёв очередей 4.2, 7.3 и 3.2 МБ требует фильтрации по балансодержателю.
- Привязка дворовых маршрутов к разбивке по участкам и к контурам АСУ ОДС (тип 38) не сделана.

## 2026-09-16 — аудит перед рабочим днём: честный риск и чистка данных

### Что сделано

- Риск по зоне теперь считается по расстоянию до объекта, а не по статусу прибора. Причина: при точности хуже 5 м фиксация целиком уходила в статус «на проверке», и признак «далеко от объекта» терялся. В отчёте было 8 рисков там, где нарушений по расстоянию — 306 из 555 фиксаций.
- Добавлена категория «Недостоверная геопривязка»: позиция, которую браузер определил по сети вместо спутника — одна точка на весь город, точность в сотни километров. Такая фиксация не считается превышением зоны: расстояние в ней ничего не доказывает.
- Клиент фотофиксации и служба не принимают позицию с точностью хуже 500 м. Клиент показывает объяснение и не отправляет снимок, служба отвечает `gps_accuracy_unusable`.
- По указанию заказчика разброс зоны вокруг объекта увеличен с ±5 до ±15 м: граница нарушения теперь 30 м вместо 20. Порог изменён в трёх местах синхронно — служба, модель клиента и модуль рисков; нормативные документы обновлены.
- Данные вычищены: удалены пять тестовых фиксаций («Тест», «Тест 2», «Тест 3», «Тест_ПП») вместе с их файлами, варианты написания исполнителей сведены к наиболее частой форме.

### Решения

- Два порога достоверности: 100 м — для отчёта, 500 м — для отказа в приёме. Первый отделяет «место нельзя подтвердить», второй — «это вообще не спутниковая позиция».
- Отсутствие точности — не отказ: фиксация принимается, но попадает в «Недостоверную геопривязку».
- Варианты написания сводятся к наиболее частой форме, а не к придуманной: список исполнителей — свободный текст, эталонного перечня нет.

### Проверки

- Тесты фотослужбы 70/70, модель клиента 29/29, питоновский набор 27/27, тесты API карты 18/18.
- Чистка выполнена скриптом с печатью состояния «до» и «после»: было 24 исполнителя, стало 15; удалено 5 фиксаций и 10 файлов (1188 → 1178).
- Три падения в первом прогоне оказались ошибками в тестовых данных, а не в коде: вспомогательная функция выдавала всем фото одинаковый отпечаток, а два теста опирались на статус вместо расстояния.

### Остаток

- Оповещения не работают и не могут работать: бот не создан, а в конфигурации службы оповещений вместо токена лежит заглушка «ЗАМЕНИ:…». Нужен токен от BotFather — сейчас префектура не получает ни новых фото, ни новых наборов правок.
- У службы нет резервного копирования и мониторинга: в автозадачах только задания соседнего проекта, пример из комплекта не установлен. Оставлено без изменений по указанию заказчика.
- Точность GPS хуже 5 м у большинства фиксаций сохраняется: решение о том, считать ли это риском, за заказчиком.

## 2026-09-16 — выкатка опознания объектов и типов ДКМ/ТУ в API

### Что выкачено

- Коммит `ea0ef31` отправлен в `origin/main`. GitHub Pages собрался (run 130, success), проверки атласа и СММ прошли (run 33, success). Публичные страницы отдают новую версию: `object-labels.js` на месте, в `district-changes.js` больше нет `dkm_route_yards`, `api-config.js` пробрасывает причины отказа.
- Исходники API на хосте обновлены архивом из закоммиченного состояния в `/opt/odh-sao`, образ `odh-sao-api` пересобран, контейнер пересоздан. Хост не является git-чекаутом; `.env` не перезаписывался.

### Найденная причина «не загружается один из маршрутов»

- В развёрнутом API типов ДКМ и ТУ не было вовсе: `grep dkm_route` по `/opt/odh-sao/api/lib/validation.js` не находил ничего, миграции ограничивались 001–002. Любой маршрут ДКМ или ТУ отклонялся с 422, при этом клиент проверку проходил — район видел зелёную проверку и отказ при отправке.
- Расхождение возникло 15 сентября: типы добавили в репозиторий, но контейнер API после этого не пересобирался (это было записано в «Остатке» записи о маршрутах ДКМ и ТУ).
- Дополнительно клиент отбрасывал список причин отказа: `api-config.js` брал только `error` и терял `details`, поэтому район видел фразу «Набор не прошёл проверку» без указания объекта. Исправлено в этом же коммите.

### Проверки после выкатки

- Внутри контейнера: `dkm_route` присутствует, `dkm_route_yards` отсутствует, миграция `003_single_prefecture_role.sql` на месте.
- Миграция 003 применилась при старте: ограничение роли сузилось до `district_editor`/`prefecture_admin`. Учётные записи не изменились — 16 районных и 1 префектуры, роль `reviewer` в базе отсутствовала.
- Наборы: на момент выкатки было 2 записи в `submissions`, фото-меток 0. Существующие записи не затронуты; третий набор появился уже после выкатки и описан ниже.
- Публичный smoke: `https://obhod-sao.ru/odh-api/api/health` → 200. Остальные контейнеры (`jirajura-*`, `sao-photo-service-*`, `odh-sao-postgres`, `odh-sao-notify`) не перезапускались.

### Точка отката

- Образ: тег `odh-sao-api:rollback-20260916` (тот же ID, что был у `latest` до пересборки).
- Исходники и compose: `/var/backups/odh-sao/api-code-before-20260916.tar.gz` (режим 600).
- Откат: `docker tag odh-sao-api:rollback-20260916 odh-sao-api:latest && cd /opt/odh-sao && docker compose -f docker-compose.production.yml up -d api`.

### Сверка образов и подтверждение

- Старый образ знал восемь типов: `queue`, `rotor_transfer`, `temporary_snow_storage`, `rotor_snow_storage_zone`, `dry_snow_dump`, `pgm`, `smm_storage`, `other`. Новый — одиннадцать: добавились `dkm_route`, `tu_route`, `tu_route_yards`.
- Проверка в работающем контейнере на настоящей границе САО: `queue`, `rotor_transfer`, `dkm_route`, `tu_route`, `tu_route_yards` принимаются, `dkm_route_yards` отклоняется с понятным сообщением.
- Набор отклоняется целиком при одном неверном объекте. Поэтому один маршрут ДКМ блокировал отправку всего набора — так это и выглядело для исполнителя: «не отправляется маршрут».
- Подтверждение исполнителя после выкатки: тот же набор отправлен и отобразился. В базе он записан 10:21 UTC, через шесть минут после пересборки контейнера (10:15 UTC), и содержит `dkm_route` ×2, `tu_route_yards`, `rotor_transfer`, `rotor_snow_storage_zone`, `pgm` ×3, `smm_storage`.
- Этот набор затем отклонён приёмкой с пустым комментарием. Отклонить набор без причины интерфейс позволяет, хотя памятка префектуры требует писать причину. Отдельно: у отклонённого набора в интерфейсе приёмки не остаётся кнопок решения, поэтому передумать и утвердить его из интерфейса нельзя — только повторной отправкой района.

### Остаток

- Статическая копия атласа в `/opt/odh-sao` остаётся от 12 сентября. Пользователи её не видят: страницы отдаёт GitHub Pages, а хостовая копия служит только контекстом сборки образа API.
- Оповещения по-прежнему не работают (нет токена бота) — это отдельная задача, выкатка её не касалась.

## 2026-09-16 — оповещения в Telegram включены

### Что сделано

- Бот создан пользователем: `@CMCOS_bot`. Токен и чат внесены в `/opt/odh-sao/.env` (`TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_IDS=254200036`), перед правкой создана копия `.env.bak-before-telegram-20260916` с режимом 600. Секреты в репозиторий не попадали.
- В `docker-compose.production.yml` службе `notify` добавлено `extra_hosts` с закреплением адреса `api.telegram.org` → `149.154.167.220`.
- Контейнер `odh-sao-notify-1` пересоздан; служба поднялась с сообщением «Приём событий слушает порт 8790. Разрешённые чаты: 254200036».

### Почему не работало

- В `.env` вместо токена и чата лежали заглушки: значение токена было 30 символов вместо 46, поэтому даже при живом контейнере отправлять было некуда.
- Контейнер разрешал `api.telegram.org` только в IPv6 (`2001:67c:4e8:f004::9`), а исходящего IPv6 у хоста нет — запросы падали с `UND_ERR_CONNECT_TIMEOUT`.
- Разрешение по IPv4 тоже не помогало: адрес `149.154.166.110`, который отдаёт DNS, с этого хоста недоступен. При этом `149.154.167.220` и `149.154.175.100` открыты и отдают настоящий API Telegram. Блокировка по конкретным адресам, а не по контейнерам: оба контейнера видят одинаковую картину, а обычный интернет (github 200) доступен везде.

### Проверки

- `getent hosts api.telegram.org` внутри контейнера отдаёт закреплённый `149.154.167.220`.
- Служба приняла событие и доставила его: `POST /event` → `{"delivered":1}`.
- Связка от API проверена теми же переменными, что использует сервис: запрос из контейнера `odh-sao-api-1` по `NOTIFY_URL`/`NOTIFY_SECRET` → `{"delivered":1}`.
- Сообщения доставлены в чат 254200036 (подтверждено ответом Telegram с `message_id`).

### Что теперь приходит в Telegram

- Карта ОДХ: «Набор правок на приёмку: <район>» с кнопками «Утвердить» и «Отклонить» — решение можно принять прямо из чата.
- Фотослужба: «Новое фото от района» (район, объект, исполнитель, вердикт GPS, удаление в метрах), «Префектура подтвердила фото» / «Префектура отклонила фото» с причиной, «Сбой обработки запроса» уровня critical.
- Вручную через службу или `notify/bin/notify.js` можно отправить произвольное событие, вопрос с вариантами (`/ask`) и запрос действия (`/action`).

### Остаток

- Список получателей — один чат (254200036). Чтобы оповещения получали другие сотрудники, их идентификаторы нужно дописать в `TELEGRAM_CHAT_IDS` через запятую и пересоздать контейнер `notify`.
- Адрес `149.154.167.220` закреплён вручную. Если Telegram его сменит или провайдер начнёт блокировать и его, оповещения снова замолчат — признак виден по `fetch failed` в `docker logs odh-sao-notify-1`.
- Порт 8790 не публикуется на хост: служба оповещений доступна только изнутри docker-сетей.

## 2026-09-16 — районы не могли войти: ограничитель считал адрес прокси

- Районы прислали «Вход не выполнен: too_many_login_attempts». Ключом ограничителя был адрес сокета (`clientKey` в `server.js`), а служба стоит за прокси: адрес у всех один, поэтому десять неудачных входов откуда угодно запирали весь округ на 15 минут. Два района упёрлись в это одновременно.
- Настоящий адрес клиента прокси уже передаёт в `X-Forwarded-For` (`sao-photo-location.conf`), но служба его не читала.
- Ключ вынесен в `src/client-address.js`: берётся последний адрес цепочки (его дописывает прокси, всё левее клиент может подставить сам), без заголовка — адрес сокета.
- Проверка на боевой службе: двенадцать неудачных входов с адреса 10.0.0.1 дают десять 401 и затем 429, а с другого адреса 10.0.0.2 — снова 401. Счётчики раздельные, защита от перебора сохранилась.
- Пересборка контейнера сбросила прежние блокировки: районы могут входить сразу.
- Тесты службы 75/75; добавлены четыре на разбор адреса.

## 2026-09-16 — лист «На штаб» в выгрузке фотофиксации

- В книге Excel появился отдельный лист «На штаб» — короткая форма ОУИФР: те же объекты по типам (остановки, пешеходные переходы, подъезды), у каждой категории свой План, Факт и процент, отдельно по районам и в строке ИТОГО. Ниже — тот же объём работ по балансодержателям, включая «АвД САО». Большая книга и остальные листы не менялись.
- В шапке у каждой категории свой бледный цвет; смысл несёт название, а не цвет.
- Проверено на боевой выгрузке: в ИТОГО по САО остановки 806/39 — 4,8 %, ПП 427/28 — 6,6 %, подъезды 10 035/1 213 — 12,1 %, всего 11 268/1 280 — 11,4 %.
- Служба пересобрана; точка отката — тег `sao-photo-service-photo-service:rollback-20260916` и копия `exports.js` в `/var/backups/odh-sao/`.

## 2026-09-16 — единица учёта выгрузки: отметка точки, а не уникальный объект

- Заказчик сообщил, что выгрузка считает уникальные объекты, а нужен объём отметок по каждому виду: район снимает конкретную точку на карте, и у одного перехода таких точек десятки. На карте фотофиксации видно те же 2 235 метки по ПП, а в листе стояло 427 — число уникальных `odh_id` с районом.
- Лист «На штаб» берёт план и факт из счётчиков точек: план — все отметки вида и района (`array_length(source_ids, 1)`), факт — отметки, по которым уже есть фото (`count(DISTINCT photos.source_id)`). Второй блок назван «Те же отметки по балансодержателям», примечание листа объясняет единицу учёта. Листы «Обзор», «Районы», «Объекты» и «Фотографии» не менялись: охват объектов остаётся их собственной метрикой.
- Найдено на проверке с настоящей PostgreSQL и исправлено: `jsonb_array_length(o.source_ids)` не существует для колонки `text[]`, поэтому запрос отчёта падал целиком — сводка и обе выгрузки отдавали бы 500. Заменено на `array_length(o.source_ids, 1)`.
- Разовый скрипт `scripts/backfill-photo-source-id.js` привязывает уже загруженные фото к ближайшей зарегистрированной точке объекта тем же правилом, что и живая служба (`assessDistanceRisk`). Без него факт по отметкам был бы нулевым: привязка к точке появилась только сейчас, у загруженных ранее фото её нет. Внутри объекта выбор точки район не меняет, поэтому числа районов от разовой привязки не зависят. Запуск по умолчанию dry-run, запись — `--apply`; фото без GPS остаются без привязки.
- Пересчёт по реальным наборам (`object-maps/data`, группировка как в импорте): 812 отметок остановок, 2 235 ПП, 10 035 подъездов — всего 13 082 отметки вместо 11 275 объектов. В разрезе ПП у «Аэропорта» 259 отметок вместо 56 объектов, у «Хорошевского» 263 вместо 50, у «Соколов» 146 вместо 31.
- Проверка на одноразовой PostgreSQL 16: миграции 001–004 применились, отчёт считает отметки, dry-run привязки вернул 4 фото из 5 (одно без GPS), после `--apply` факт в отметках сошёлся, лист «На штаб» собран целиком. Контейнер и данные после проверки удалены.
- По просьбе заказчика в столбцы «%» добавлен светофор: пороги те же, что у полосы статуса (<33 %, 33–<66 %, ≥66 %), цвета бледные и те же, что в PDF-сводке. Сделано родным условным форматированием Excel, как и полосы: правило не срабатывает на пустой ячейке, поэтому строка без данных остаётся без цвета. Проверено разбором XML книги: 8 блоков `conditionalFormatting` на четыре столбца в обеих таблицах, у правил свои приоритеты, заливка и шрифт лежат в `dxfs`.
- `npm test` — 79/79; добавлены тесты на счёт отметок в сводке, на лист «На штаб» с многотоповым переходом (43 точки) и на светофор. Документация службы и ChangeLog обновлены.
- Выкат не выполнялся на момент правок. Порядок для боевой службы: применить миграцию `004_photo_source_point.sql`, прогнать привязку `--apply`, пересобрать контейнер, проверить выгрузку. До миграции новый код отчёта запускать нельзя.

## 2026-09-16 — выкат: «На штаб» в отметках и светофор на проценты

- Бэкап перед изменениями: `sh scripts/backup.sh` → `/var/backups/sao-photo-service/20260916T121630Z`, `sha256sum -c` подтвердил `database.dump` и `media.tar.gz` (758 МБ).
- Состояние до правок: 11 275 объектов, 1 733 фиксации, колонки `photos.source_id` нет, миграции 001–003. Боевой каталог `/opt/sao-photo-service/photo-service` не под git, поэтому файлы выложены копированием, md5 сверены до и после заливки; образ пересобран до перезапуска, работающая служба не прерывалась.
- Применены миграции 001–004 (`photos.source_id text`). Привязка: dry-run 1 735 фото, затем `--apply` — 1 736 привязано, пропусков нет (у всех фиксаций есть GPS и точки объекта).
- Проверки после привязки: без привязки 0, ссылок на точку чужого объекта 0, всего 1 742 фиксации. Повторный прогон поймал 4 фото, загруженных в момент переключения контейнера; дальше привязка не нужна — новые фиксации получают отметку при загрузке (проверено: снимки после старта контейнера в 12:19 UTC приходят с `source_id`).
- Выкат: `docker compose -p sao-photo-service up -d photo-service`; health 200, `database=connected`, контейнер healthy.
- Живая проверка листа «На штаб» на боевых данных: план 806 остановок / 2 233 ПП / 10 035 подъездов = 13 074 отметки, плюс 8 отметок в строке «Не распределено»; факт 66 / 93 / 1 398 = 1 557 отметок, 11,9 %. Второй блок по балансодержателям даёт 13 082 отметки — это все объекты САО, включая нераспределённые, и сходится с числом точек в наборе. Светофор: 8 блоков условного форматирования (`E4:E21`, `H4:H21`, `K4:K21`, `N4:N21` и такие же во втором блоке), по 3 правила.
- Полная книга на боевых данных собирается целиком: 45,8 МБ за 6,7 с, листы «Обзор», «На штаб», «Районы», «Динамика», «Риск» (689 строк), «Объекты», «Фотографии» (1 745); PDF — 11 КБ, корректен. Охват на момент проверки: 1 504 объекта с фото из 11 268, подтверждённых приёмкой пока нет, поэтому выполнение 0 %.
- Замечание по данным: у части фиксаций место съёмки далеко от объекта — до 22 км (635 в радиусе 15 м, 428 до 30 м, 673 дальше). Это отдельная категория «Превышение зоны»; привязка к ближайшей точке такие строки не маскирует.
- Точка отката: коммит `2587744` (код до правок) и тег образа `sao-photo-service-photo-service:rollback-20260916`. Откат кода — вернуть файлы и пересобрать контейнер; колонка `source_id` старому коду не мешает.

## 2026-09-16 — «На штаб» по эталону заказчика: районы, «АвД САО», светофор и комментарий

- Заказчик прислал эталонный образец выгрузки (файл `ЭТАЛОН.xlsx` и скриншоты). Изучены значения, формулы, объединения, заливки, шрифты, границы и правила листа «Sheet1» — это и есть требуемая раскладка «На штаб».
- Раскладка перенесена: строка групп («Автобусные остановки», «Пеш.переход», «Подъезды (Вх. гр.)», «Итого») с бледными заливками, под каждой категорией «План»/«Факт»/«%», столбцы «№» и «Район» без прежнего «Жилищник «Район»», строка ИТОГО по САО, шрифт Century Gothic, рамка по всем сторонам, выравнивание по центру и середине (просьба заказчика отдельным сообщением).
- Ниже первой таблицы идёт вторая — те же числа, отсортированные по «Итого: факт»: в первой ячейке стоит формула-массив `SORT(B3:N…,12,0)`, как в эталоне. Значения под формулой записаны сразу, поэтому файл открывается и там, где динамических массивов нет.
- Объекты с балансодержателем «АвД САО» (574 остановки) вынесены в отдельную строку «АвД САО» и больше не считаются району, на территории которого стоят; строка ИТОГО — сумма строк таблицы. Остальные балансодержатели (ДЭУ, жилищники) по-прежнему идут в счёт района, как раньше.
- Проценты подсвечены светофором по цветам эталона: ноль — `EA9999`, меньше 33 % — `F4CCCC`, 33–<66 % — `FFFFF2CC`, от 66 % — `D9EAD3`. Сделано условным форматированием, число остаётся чёрным, пустая ячейка цвета не получает. Прежняя реализация с цветным шрифтом и порогом без учёта нуля заменена.
- Под таблицами появился готовый текст комментария для рассылки: «Коллеги, добрый день! Слабая динамика по оцифровке объектов! Следующим районам срочно приступить к данной задаче:» и список районов, где не закрыто ни одной отметки. Формулировка взята из эталона комментария заказчика дословно, меняется только список.
- Осознанные отклонения от эталона (о них доложено): лист называется «На штаб», а не «Sheet1»; проценты — числа с форматом `0,0 %`, а не текст (иначе светофор не считается по значению); у процентных ячеек чёрный шрифт вместо белого из эталона; убраны блок по балансодержателям и строка «Не распределено по районам», поэтому 8 отметок без района в таблицу не попадают и остаются в листах «Объекты» и диагностике.
- `npm test` — 79/79; тесты листа переписаны под новую раскладку: шапка, строка «АвД САО», ИТОГО, формула `SORT`, комментарий, светофор из четырёх ступеней, выравнивание и рамки.

## 2026-09-16 — ежечасная сводка штаба картинкой в Telegram

- Заказчик попросил, чтобы бот каждый час присылал в тот же чат картинку со второй (отсортированной) таблицей листа «На штаб» и комментарием к ней; параметры подтвердил: круглосуточно, вторая таблица, тот же чат.
- Числа листа и картинки вынесены в общую модель `src/headquarters.js` (`headquartersBoard`): районы, «АвД САО», итог, порядок по «Итого: факт», список районов без закрытых отметок и строки комментария. Лист Excel и картинка берут числа оттуда, поэтому разойтись они не могут.
- Картинка рисуется в `src/digest.js` через `@napi-rs/canvas` (прекомпилированный бинарник, в `package-lock.json` есть вариант под alpine) шрифтом Jost из `assets/fonts`. Знака «№» в Jost нет — он рисуется составным: латинская «N», маленькая «o» и подчёркивание. Модуль отрисовки подключается лениво, поэтому сбой картинки не трогает API.
- Служба оповещений получила маршрут `POST /photo` (base64 PNG, подпись, имя файла) и отправку `sendPhoto` multipart-запросом; лимит тела для картинки поднят до 8 МБ. Проверено тестами службы и живой отправкой.
- Расписание: при `PHOTO_SERVICE_DIGEST_ENABLED=true` служба запускает отправку ровно в начале каждого часа, 24 часа в сутки. Руками — `node scripts/send-digest.js` (или с `--out файл`, чтобы посмотреть картинку без отправки).
- По замечаниям заказчика к таблице: объекты без района учитываются в строке «АвД САО» — ИТОГО теперь сходится со всем набором (812 остановок, 2 235 точек ПП, 10 035 подъездов = 13 082 отметки); проценты показываются всегда и округляются до целого, светофор не красит ячейки без плана, иначе в таблице «пропадали» числа.
- Проверки: `npm test` в фотослужбе 81/81, в службе оповещений 40/40. На боевых данных картинка собрана и просмотрена (1386×809, 137 КБ): строка «АвД САО» 576/17, ИТОГО 812/84 — 10 %, 2 235/106 — 5 %, 10 035/1 687 — 17 %, всего 13 082/1 877 — 14 %. Тестовое сообщение отправлено в чат 254200036; после пересборки опрос Telegram восстановился (единственная ошибка — сразу после старта контейнера).

## 2026-09-16 — ДЭУ уходят на АвД, нижняя таблица сортируется по проценту, комментарий — разбор выгрузки

- Заказчик приложил источник ПП и потребовал: все пометки с балансодержателем ДЭУ считать АвД САО. В файле `Коордианаты_ПП.xlsx` (лист `intersection`, колонка «Баланс») подтверждены ровно три значения: «ДЭУ 1» (320 записей), «ДЭУ 2» (450) и «ДЭУ 3» (559); остальные значения — названия районов.
- Правило учёта дополнено: в строку «АвД САО» идут объекты с балансодержателем «АвД САО», с балансодержателем вида «ДЭУ N» (проверка `isAutodorHolder`) и объекты без района. Район по-прежнему снимает эти объекты, но объём работ считается владельцу.
- На боевых данных строка «АвД САО» выросла до 576 остановок и 1 331 точки ПП (1 907 отметок, 49 закрыто), у районов точки ПП уменьшились: у Аэропорта 259 → 43, у Хорошевского 263 → 97. ИТОГО не изменилось — 812 / 2 235 / 10 035 = 13 082 отметки.
- Сортировка нижней таблицы переведена с «Итого: факт» на «Итого, %» (13-я колонка диапазона): так просил заказчик, показывая отсортированные «Итого» столбцы. Формула `SORT(B3:N…,13,0)` и записанные значения сортируются одинаково, совпадающие проценты разводятся по числу закрытых отметок и названию — порядок устойчивый, строки не «гуляют». Верхняя таблица остаётся в алфавитном порядке и служит для сверки между выгрузками.
- Комментарий к выгрузке больше не шаблон: он собирается по числам — общий процент и количество отметок, районы без единой закрытой отметки (с просьбой приступить), лидеры и самая слабая категория. Подпись в Telegram и текст под таблицей берут одни и те же строки.
- Проверки: `npm test` в фотослужбе 81/81. Свежая сводка отправлена в чат: «2 112 из 13 082 отметок — 16 %», отстают Войковский, Дмитровский, Левобережный, Сокол, Тимирязевский; впереди Восточное Дегунево 67 %, Западное Дегунино 61 %, Савеловский 54 %; слабее всего пешеходные переходы (5 %).
- По просьбе заказчика комментарий открывается фиксированной строкой «Направление — «Оцифровка объектов САО» — дата и время», затем пустая строка и разбор. Строки уходят и подписью к картинке, и текстом под таблицами — формат один. Заголовок картинки справа больше не повторяет дату, а называет порядок: «районы по убыванию «Итого, %»».

## 2026-09-16 — четыре столбца на вид и фото, привязанное к точке

- Заказчик попросил в штабной таблице видеть и объём точек («как на сайте размечено»), и число объектов по ID: в каждой категории стало четыре столбца — «Точек», «Объектов», «Факт», «%». Лист расширен до 18 колонок, формула сортировки нижней таблицы перешла на 17-ю колонку диапазона (`SORT(B3:R19,17,0)`), светофор работает по столбцам «%» (F, J, N, R), картинка в Telegram рисуется в той же раскладке.
- Боевые числа: 812 отметок и 812 объектов по остановкам, 2 235 и 428 по переходам, 10 035 и 10 035 по подъездам — всего 13 082 отметки на 11 275 объектов; закрыто 2 168 отметок (17 %). Строка «АвД САО» — 1 907 отметок на 766 объектов (после переноса ДЭУ).
- Найден и устранён дефект, о котором сообщил заказчик: фото, сделанное на одной точке перехода, показывалось на соседних точках того же объекта. Причина двойная — служба отдавала по `sourceId` все снимки объекта, а клиент считал покрытие по объекту. Теперь сервер отдаёт снимки только запрошенной точки, в отчёт добавлен `sourceId` каждого кадра, а клиент считает покрытие по точкам: норма — одно фото на точку. Тесты доказывают, что снимок одной точки не закрывает соседние.
- Связь сервера с Telegram оказалась нестабильной: примерно половина попыток обрывается по таймауту (проверено серией замеров — то же самое видно и с хоста, и из контейнеров, независимо от сети). Это не код: провайдер. В службу оповещений добавлены повторы отправки (4 попытки с нарастающей паузой), ошибки самой службы Telegram при этом не повторяются. Из-за этого сбоя первая отправка сводки упала с HTTP 500 — после повторов ушла.
- Проверки: `npm test` в фотослужбе 81/81, в службе оповещений 43/43 (добавлены тесты повторов), модель клиента 29/29. Сводка отправлена в чат.

## 2026-09-16 — три колонки, учётка АвД, охват в дашборде, время МСК и аудит

- Штабная таблица вернулась к трём колонкам на вид: «Объекты» (отметки к отработке — столько же показывает страница фотофиксации), «Факт», «%». Колонка объектов по ID убрана, формула сортировки снова `SORT(B3:N19,13,0)`, светофор в столбцах E/H/K/N. ИТОГО: 812/125 — 15 %, 2 235/136 — 6 %, 10 035/2 020 — 20 %, всего 13 082/2 281 — 17 %; «АвД САО» — 1 907/62.
- Появилась учётка владельца «АвД САО» (роль `district_editor`, район «АвД САО»). Её зона — объекты владельца, объекты ДЭУ и объекты без района по всему округу; правило вынесено в `src/scope.js` и общее с выгрузкой и картинкой. Районная учётка по-прежнему видит только свой район.
- Дашборд клиента переведён на охват: главный процент и полосы районов считаются по объектам с фото, а не только по подтверждённым приёмкой. Раньше весь экран показывал нули, хотя районы уже загрузили 2 518 снимков: подтверждений приёмки пока нет вовсе. Рядом остались числа «На проверке» и «Подтверждено приёмкой».
- Службы и база переведены на московское время: хост уже был в Europe/Moscow, контейнерам добавлен `TZ=Europe/Moscow` (в отчётах и сводке время теперь МСК, было UTC).
- Аудит затронутых направлений на боевой службе:
  - данные: 11 275 объектов / 13 082 отметки (812/812, 428/2 235, 10 035/10 035); фото 2 518, все привязаны к своей точке, без привязки — 0, ссылок на чужую точку — 0, отклонённых — 1; объектов без района — 7;
  - зона АвД: 766 объектов / 1 907 отметок, закрыто 62;
  - учётки: 16 районных + АвД + префектура; для АвД проверено живьём — вход, свои объекты 200, чужие 404, сводка 759 приписанных + 7 без района;
  - выгрузка: полная книга 67,6 МБ за 7,9 с, 7 листов (Риск 993, Объекты 11 276, Фотографии 2 518), PDF корректен, ИТОГО сходится с базой;
  - сводка: картинка 163 КБ, подпись с фиксированной строкой, время МСК;
  - клиент: страница открывается, под АвД видны свои 576 объектов, карта ограничена ими, сводка — охват 8 % (58 из 759), в консоли только ожидаемый 401 без входа;
  - тесты: фотослужба 86/86, оповещения 43/43, модель клиента 31/31.
- Открытые наблюдения: «Районы округа» в дашборде скрыты для районных учёток (для АвД тоже — включается одной правкой, если нужно); норма «одно фото на точку» задаётся константой; связь с Telegram у провайдера нестабильна, спасают повторы отправки.
- Отдельная просьба районов на будущее: в разметке маршрутов ОДХ видеть историю своих отправок (какие маршруты отправляли).

## 2026-09-16 (вечер) — штаб в PDF, топы по нарушениям, отчёт по маршрутам ОДХ

### Что сделано

- **Штабная таблица выгружается отдельным PDF.** Модуль `photo-service/src/pdf-headquarters.js` — печатная форма листа «На штаб»: сверху районы в исходном порядке (по ней сверяют числа с выгрузкой), ниже та же таблица по убыванию «Итого, %» (она уходит в штаб), затем комментарий для рассылки и примечание. Числа берёт общий `src/headquarters.js`, поэтому лист, картинка для Telegram и PDF не расходятся. Кнопка «PDF: таблица на штаб» рядом с Excel-выгрузкой.
- **Район объекта определяется балансодержателем.** Правило вынесено в `src/scope.js` (`reportingDistrict`) и применено ко всем сводным разрезам: `summarizeByDistrict`, штабная таблица, выгрузка, дашборд. Объекты «АвД САО», «ДЭУ N» и объекты без района считаются за АвД. Следствие: `overall` в `reportPayload` теперь считается по всему набору, иначе ИТОГО районов не сходилось с общим числом объектов (11 268 против 11 275).
- **Проценты в отчётах — целые** (`percentLabel`): и в «Обзоре», и в «Районах», и в обоих PDF.
- **Топы по нарушениям.** Новый модуль `riskTops` в `src/risks.js`: районы по числу рисков и исполнители внутри каждого района. Отдаётся в `/reports/summary` (поле `riskTops`), выводится листом «Топы» в Excel, двумя разделами в кратком PDF и блоком в дашборде клиента.
- **Вложение сводки — только таблица.** Из картинки убраны заголовок и текст комментария: комментарий уходит текстом поста. Исправлен дефект подписей колонок: тройка «Объекты / Факт / %» бралась по модулю 4 и на каждой следующей группе съезжала, выдавая `undefined`. Теперь подпись считается по смещению внутри группы.
- **Упоминание ответственного в подписи сводки**: `NOTIFY_MENTION`, по умолчанию `@TomGruz200`, последней строкой после комментария.
- **Бот умеет отправлять файл.** `notify`: `sendDocument` (multipart, тот же retry, что у `sendPhoto`), маршрут `POST /document`, метод `notifier.document`; в клиенте api — `document()`.
- **Отчёт по отрисовке маршрутов ОДХ раз в 3 часа.** `api/lib/route-report.js` — чистый модуль (разбор `change_set -> features` по видам и статусам, CSV с BOM и «;», текст для Telegram с фиксированной первой строкой). Выборка — `repository.routeReportRows()` (разворачивает `jsonb_array_elements`, `COALESCE` защищает от набора без `features`). Endpoint-ы `GET /api/reports/routes` и `GET /api/reports/routes.csv` (только префектура). Планировщик выровнен на 00:00/03:00/… под флагом `ODH_ROUTE_REPORT_ENABLED`, есть разовый CLI `scripts/send-route-report.js`.
- **Исправлено при проверке на проде:** отчёт показывал только районы с отправками, поэтому «без маршрутов» выходило пустым там, где работа не начата — теперь список районов округа передаётся явно. Контейнеру api задан `TZ=Europe/Moscow`: без него время в сводке и CSV печаталось по UTC (16:24 вместо 19:24).

### Боевые числа на момент отчёта

- Маршруты ОДХ: 6 наборов от 3 районов из 16; 3 на приёмке, 3 отклонено, **утверждено 0**. Маршрутов 10, зон 1, точек 12 (всего 23 объекта). Аэропорт 5, Головинский 3, Дмитровский 2; без маршрутов — 13 районов.
- Фотофиксация: 13 082 отметки, закрыто 2 460 (19 %). Фото 2 745, **без привязки к точке 0**, закрыто 2 460 точек на 2 342 объектах.

### Проверка фикса «фото принадлежит только своей точке»

Заказчик просил подтвердить, что снимок одной точки не подтягивается на соседние точки того же объекта. Проверено в коде и на боевой базе: у перехода «Тимирязевская улица · ОДХ 10002605» 34 точки, закрыта **одна** (2 фото) — раньше один снимок закрывал весь объект. Отдельно: 4 хеша встречаются на разных объектах — это ровно то, что ловит категория «Дубль фото на разных объектах».

### Порядок выкладки и точки отката

1. `git push origin main` → GitHub Pages (клиент и атлас).
2. Фотослужба: копирование `src`, `server.js`, `scripts` в `/opt/sao-photo-service/photo-service`, затем `docker compose -p sao-photo-service build photo-service && up -d photo-service`.
3. api и notify: копирование `api`, `notify` в `/opt/odh-sao`, затем `docker compose -f docker-compose.production.yml build api notify && up -d api notify`.
4. `docker-compose.production.yml`: контейнеру api добавлен `TZ=Europe/Moscow` и проброс `ODH_ROUTE_REPORT_ENABLED`; в `/opt/odh-sao/.env` флаг выставлен в `true`.
- Точки отката: коммит `168f10a` (состояние до этой правки), `/var/backups/deploy-20260916T192113Z` (src фотослужбы, api, notify, compose), `/var/backups/deploy-*-api.tgz`, `/var/backups/docker-compose.production.yml.bak-*`, `/var/backups/env.bak-*`.

### Проверки

- Тесты: фотослужба **97/97**, api **34/34**, оповещения **52/52**, модель клиента **35/35** — всего 218.
- Живьём: `/api/health` отвечает; сводка штаба ушла в чат с упоминанием и московским временем (19:27); отчёт по маршрутам ушёл текстом и CSV; время во всех трёх контейнерах — МСК (проверено `node -e` внутри контейнеров: shell `date` в alpine показывает UTC, потому что в образе нет tzdata, а Node считает время по своим данным).

### Открытые наблюдения

- **Список районов ОДХ короче на один район.** В `odh-map/district-changes.js` и `api/lib/validation.js` 16 районов, Щукино отсутствует, хотя в фотофиксации округ идёт по 17 районам (включая Щукино). Значит, у Щукино нет учётки на карте ОДХ, оно не может отправить разметку и никогда не попадёт в отчёт по маршрутам. Нужно решение заказчика: добавлять Щукино в ОДХ или это осознанное ограничение.
- Строка «АвД САО» на доске районов клиента кликабельной не сделана: фильтр по району на сервере ищет `district = 'АвД САО'`, а таких объектов нет. Чтобы строка стала переходом, серверной выборке нужен отдельный режим «зона АвД».
- Ежечасная сводка штаба и отчёт по маршрутам теперь идут в один чат; при желании их можно развести по разным чатам — сервис оповещений принимает список получателей.

### Разбор обращения «район не может войти»

Район сообщил «Вход не выполнен: invalid_credentials» на странице фотофиксации. Проверено по шагам:

- учётка `войковский` есть в базе, `active = true`, пароль в базе не пустой — отказ не из-за блокировки и не из-за троттлинга (тот отдаёт `too_many_login_attempts`);
- пароли из файла учёток заказчика сверены с хешами базы `scrypt$N$r$p$соль$хеш` — **совпали 17 из 17**;
- живой вход по публичному адресу `https://obhod-sao.ru/photo-api/auth/login` для всех 17 учёток фотослужбы — **HTTP 200**, и для всех 17 учёток карты ОДХ (`/odh-api/api/auth/login`) — тоже **200**.

Вывод: служба исправна, пароли действительны. Причина — ввод пароля: у районов два разных набора учёток (фотослужба и карта ОДХ), а пароли — 16 случайных символов в четырёх группах, которые легко перепутать или ввести с телефона неверно. Заказчику нужен один файл с пометкой, для какого сервиса каждая учётка, либо единый вход.

### Инцидент выкладки: CRLF попал в боевые файлы

- Архив для выкладки собирался из рабочей копии Windows, где `core.autocrlf=true`. В прод уехали те же файлы, но с CRLF; в бэкапе они лежали с LF, поэтому сравнение «прод против бэкапа» показывало различие во всех файлах сразу.
- Функционально это ничего не сломало (Node к CRLF безразличен, сборка прошла), но в `Dockerfile` CRLF — уже реальный риск, а разбор инцидента из-за шума сравнения затянулся: сначала я принял различие переводов строк за подмену кода в `auth.js` и ошибочно счёл, что сломал вход (я сравнивал `api/lib/auth.js` вместо `photo-service/src/auth.js` — схемы хранения паролей у служб разные).
- Исправлено: `.gitattributes` закрепляет LF для выкладываемых пакетов; архив теперь собирается `git archive` (берёт индекс, всегда LF) — команды записаны в runbook; на сервере CRLF убран из всех файлов (`sed -i 's/\r$//'`), после чего api, notify и фотослужба пересобраны, а вход перепроверен — 34 из 34 учёток отвечают 200.

## 2026-09-17 — единая выгрузка по районам

- Заказчик принёс три файла `sao-photo-report (8..10).xlsx` и попросил «сделать единую выгрузку по району». Разбор показал: это одна и та же выгрузка района Головинский, скачанная трижды за минуту (1122 объекта, 1050 Головинский + 72 АвД, на штаб 444 из 1240 отметок — 36 %). Уточнил формат — заказчик выбрал «один файл, лист на каждый район».
- Добавлена выгрузка `/reports/export-districts.xlsx` (`buildDistrictsExcel` в `src/exports.js`) и кнопка «Excel: по районам». Состав: «Сводка по районам» (объекты, отметки, закрыто, %, с фото, охват, на проверке, риски по каждому району; от лучших к худшим, ИТОГО по САО), эталонный «На штаб», «Топы» и по листу на каждый район — объекты района и его фотографии с превью. Числа берёт общая штабная модель (`headquartersBoard`/`headquartersValues`), поэтому сводка, «На штаб» и листы районов сходятся; район объекта считается через `reportingDistrict`, поэтому объекты владельца, «ДЭУ» и без района попадают на лист «АвД САО».
- Боевой файл собран с живой службы: **20 листов, 103 МБ, 15,5 с**; ИТОГО сходится со штабной таблицей — 11 275 объектов, 13 082 отметки, 4 460 закрыто (34 %). Сводка по районам: впереди Восточное Дегунино 96 %, Бескудниковский 94 %, Западное Дегунино 81 %; в конце Войковский 0 (не начал), Сокол 1 %, Беговой 3 %; «АвД САО» — 5 %.
- Замечания по надёжности: генерация держит книгу в памяти службы и занимает около 11–15 с, ответ весит больше 100 МБ. Прокси допускает это (`proxy_read_timeout 120s`), первый мой запрос оборвался по `ECONNRESET` только потому, что контейнер в этот момент перезапускался после пересборки; повторный запрос прошёл. Если книга вырастет, стоит отдавать её файлом с диска, а не собирать на каждый запрос.
- Временная копия пароля префектуры, использованная для проверки входа, с сервера и с рабочей машины удалена; в репозиторий секреты не попадали.
- Тесты: фотослужба **98/98** (добавлен тест единой выгрузки по районам).

## 2026-09-17 (вечер) — выгрузки с карты ОДХ

### Что сделано

- **Запрос заказчика.** «Сделай для данной таблицы выгрузки по аналогии с object-maps» — со ссылкой на `odh-map/index.html` и образцом `object-maps/`. На уточняющие вопросы заказчик ответил: выгружать все три среза (реестр объектов, отчёт по маршрутам, сводка по районам «на штаб»), файлы собирать **в браузере из слоёв карты**, без бэкенда, логина и деплоя сервиса.
- **Новый модуль `odh-map/exports.js`** — браузерный IIFE `window.ODHExports`: чистые функции без DOM, сети и карты (разбор по районам, две единицы счёта, подтверждение источника, проценты, CSV, книга Excel, печатная форма). Рядом с `district-changes.js`, `route-visuals.js` и `object-labels.js`, тем же способом подключения.
- **В сайдбар карты добавлен раздел «Выгрузки»** (`odh-map/index.html`): `Excel: полный реестр`, `Excel: таблица на штаб`, `PDF: таблица на штаб`, `CSV: отчёт по маршрутам`. Кнопки выключены до загрузки слоёв, после ошибки загрузки остаются выключенными.
- **Данные берутся из уже загруженных слоёв.** `addLayer` теперь складывает отфильтрованный GeoJSON в `layerData`, выгрузка читает его. Ни одного нового запроса к сети, кроме CDN-библиотеки.
- **ExcelJS 4.4.0 с jsdelivr** — единственная новая внешняя зависимость страницы (рядом с Leaflet, Font Awesome и Яндекс-подложкой). Если библиотека не загрузилась, кнопка сообщает об этом и карта продолжает работать.
- **Единица счёта — точка** (геометрическая часть), а не уникальный объект: у одной позиции ОДХ бывает несколько частей. План, факт и процент считаются по точкам; «Объектов по ID» показывается рядом как вторая единица. То же правило, что в выгрузках фотофиксации.
- **Факт — подтверждение официальным источником**, а не наличие в файле. Правила по слоям: ОДХ — `status = «Утвержден»` (источник «Очередность_уборки_округ.xlsx»); ПГМ — `geometry_status ≠ odh_geometry_proxy`; СММ и гидранты — без пометки «требуется служебная сверка»; места складирования снега — `geocode_status ≠ candidate`; сухие свалки и здравоохранение — `verification_status` начинается с «подтверждено».
- **Район считается по балансодержателю** слоя (`customer` у ОДХ, `institution` у ПГМ, `district` у СММ и снега, `organization` у гидрантов). Названия в источниках разные («Жилищник Беговой», «ГБУ «Жилищник района Беговой»», «Беговой»), нормализация одна на все файлы; неразобранное и пустое уходит в строку «АвД САО» — как в соглашении `photo-service/src/scope.js`.
- **Таблица на штаб — по эталону выгрузок фотофиксации**: 14 столбцов, «№ | Район | три категории по «Объекты / Факт / %» | Итого». Верхняя таблица — устойчивый справочник (районы по алфавиту, «АвД САО» в конце), нижняя сортируется по «Итого: %», светофор бледными заливками (`EA9999` / `F4CCCC` / `FFF2CC` / `D9EAD3`), проценты целые, «ИТОГО по САО» в конце. Живая сортировка — формулой `SORT` по последней колонке, значения под ней остаются на месте.
- **Комментарий к выгрузке** повторяет форму штабной рассылки: фиксированная первая строка «Направление — «Готовность слоёв карты ОДХ» — дата и время (МСК)», приветствие, итог по точкам, районы без подтверждённых данных, лидеры и самая слабая категория.
- **PDF** собирается печатной формой в новом окне (как печатные листы `print-*.html`) — отдельной PDF-библиотеки не понадобилось.
- Имя CSV сохранено эталонным из `api/lib/route-report.js`: `odh-routes-<дата>.csv`, UTF-8 с BOM, разделитель «;», CRLF, строка «ИТОГО». Срез — только ОДХ: отчёт называется «по маршрутам», подмешивать в него контейнеры, площадки СММ и гидранты нельзя.

### Боевые числа на опубликованных слоях

- ОДХ (I + II + III очередь): **881 точка**, 688 объектов по ID, подтверждено 881 — **100 %**. Сходится с README карты (688 уникальных ID при 881 геометрической части).
- Контейнеры ПГМ: 215 точек на 110 объектах ОДХ, подтверждено **0** (все точки — `odh_geometry_proxy`, представитель геометрии).
- Прочие объекты карты: 474 точки, подтверждено 23 — **5 %** (3 сухие свалки и 20 учреждений здравоохранения).
- ИТОГО по САО: **1 570 точек, 904 подтверждено — 58 %**. Районов 17 (16 + «АвД САО»).
- Лидеры по «Итого»: Молжаниновский 94 %, Тимирязевский 92 %, Сокол 90 %; в конце Войковский и Дмитровский по 26 %, Ховрино 31 %.

### Что намеренно не выгружается

- **Суммы атрибутов слоя.** Сумма `container_count` по ПГМ даёт 223 контейнера, тогда как README карты называет 451 — это разные единицы (привязки на карте против строк реестра). Сумма `smm_units` по СММ даёт 2 529 единицы, сверять её не с чем. Выносить такие числа в файл заказчика нельзя: они читаются как расхождение с уже опубликованными цифрами. Поэтому в «Обзоре» вместо суммы атрибутов стоит колонка «Что считает вторая единица» (идентификатор ОДХ, название площадки, адрес).

### Дефекты, найденные проверками

- **ExcelJS отклоняет книгу целиком, если имя листа содержит `:`** — сборка падала на «Снег: складирование» и «Снег: сухие свалки». Листы переименованы в «Снег — складирование» и «Сухие свалки снега»; в тест добавлена проверка всех имён на `* ? : \ / [ ]` и на длину 31 символ, чтобы переименование не всплыло у заказчика при скачивании.
- **Первая редакция CSV смешивала единицы счёта**: «Объектов» считались только по ОДХ, а «Точек» — по всем слоям. Теперь срез только по ОДХ и числа аддитивны.
- **Первая редакция комментария теряла подписи категорий** — в строку «По категориям» попадало `undefined`: поле `commentTitle` не переносилось в модель.

### Отличие от эталона (осознанное)

- В фотослужбе нижняя таблица сортируется формулой `SORT(B3:N..,13,0)`. Третий аргумент `SORT` — `1` (по возрастанию) или `-1` (по убыванию); `0` не задаёт порядок и в Excel читается как возрастание, то есть худшие сверху, хотя значения под формулой отсортированы от лучших к худшим. В выгрузке карты ОДХ поставлено `-1`: смысл эталона («в штаб уходит таблица от лучших к худшим») сохранён, а формула перестала противоречить собственным значениям. Если заказчик считает `0` намеренным, правку нужно вернуть в обе выгрузки — это одна строка.

### Проверки

- Playwright `odh-map`: **28/28**, 37 с (5 новых тестов в `tests/exports.spec.js`). Новые тесты проверяют состав листов книги, 14 столбцов и светофор штабной таблицы, BOM и строку «ИТОГО» в CSV, реальное скачивание `.xlsx` (подпись ZIP и размер) и печатную форму в новом окне.
- Числа в тестах сверяются с опубликованными слоями, а не с фикстурами: 688 объектов и 881 точка по ОДХ, 1 570 / 904 / 58 % по округу.
- ExcelJS в тестах подставляется из `photo-service/node_modules` — проверка не зависит от доступности jsdelivr.

### Порядок выкладки и точки отката

1. `git push origin main` → GitHub Pages. Больше ничего: страница статическая, серверных изменений нет.
- Точка отката: коммит `163d093` (состояние до правки). Меняются только `odh-map/index.html`, новый `odh-map/exports.js`, тест, README и журналы.

### Открытые наблюдения

- **«Факт» на публичной карте — это подтверждение источника, а не выполнение работ.** Процент показывает, где координата подтверждена документом, а где это кандидат геокодера; реальное выполнение районов живёт в сервисе ОДХ (`/api/reports/routes.csv`). Если заказчику нужен разрез «план / факт по приёмке», кнопку придётся вешать на API, а не на статические слои — в браузере этих данных нет.
- Блок «ОДХ: I–III очереди» в штабной таблице всегда 100 %: все объекты ОДХ утверждены. Разброс по районам дают ПГМ, СММ, места складирования снега и гидранты, у которых пока координаты-кандидаты.
- Строки «АвД САО» и «Прочие объекты карты» стоит показать заказчику до рассылки: он видит такие числа впервые, а правило отнесения объектов к «АвД САО» уже описано в примечании под таблицей.

## 2026-09-17 (поздний вечер) — маршруты районов в сводке на штаб

### Что просили

«Нам нужно в сводке по ОДХ на штаб знать всё про маршруты — кратко и ясно». Уточнение показало, что «маршруты» — это **отрисовка районов из единой базы** (маршруты, зоны и точки с приёмкой), а не объекты уборки из слоёв карты. Срез живёт в сервисе ОДХ, поэтому одной браузерной выгрузкой его не закрыть.

### Что сделано

- **Блок «Маршруты районов (единая база)»** в листе «На штаб» и в печатной форме: по районам — маршрутов, точек, зон, на приёмке, утверждено, отклонено, время последней отправки, строка «ИТОГО», строка «Без маршрутов (N): …» и примечание о единице счёта. Районы идут в порядке службы — по убыванию маршрутов.
- **Подключение к сервису на карте**: раздел «Маршруты районов» в сайдбаре (адрес `odh-map/api-config.js`, по умолчанию `https://obhod-sao.ru/odh-api`), вход учётной записью префектуры и загрузка `GET /api/reports/routes`. Кнопка «Загрузить» и сервисный CSV закрыты, пока роли нет.
- **CSV службы отдаётся как есть** — `odh-routes-<дата>.csv` с приёмкой, тем же файлом, что уходит в Telegram.
- **Клиентская выгрузка переименована** в `sao-odh-objekty-<дата>.csv` («CSV: объекты ОДХ по районам»): раньше она называлась так же, как файл службы, а это два разных отчёта — нарисованное районами против объектов на опубликованной карте. Два разных файла с одним именем читались бы как один.

### Почему так, а не иначе

- **Блок отдельной таблицей, а не колонками в основной.** Срез приходит из другой системы и не складывается с точками карты: «маршруты районов» и «объекты ОДХ» — разные единицы, и общий «Итого» по ним был бы неверным.
- **Пустой блок не исчезает.** Если данные не загружены, на листе остаётся пометка «Данные единой базы не загружены…»: пустое место читалось бы как «маршрутов нет».
- **Проценты у маршрутов не считаются.** В сервисе приёмка считает только маршруты, а зоны и точки идут отдельными колонками, поэтому светофор к этому блоку не применяется — примечание это объясняет.

### Контракт и CORS (проверено живьём)

- Ответ `/api/reports/routes` совпал с ожидаемым поле в поле: `generatedAt`, `districts[]` (`district`, `routes`, `zones`, `points`, `submitted`, `approved`, `rejected`, `lastSubmittedAt`), `totals`, `lagging`, `csvName`.
- `OPTIONS /api/auth/login` → **204**, `Access-Control-Allow-Origin: https://hainox.github.io`, `POST` → **200**, `GET /api/reports/routes` без токена → **401**. Значит, запрос идёт из браузера напрямую, серверных правок не требуется.
- Проверить интеграцию с локального сервера нельзя: сервис разрешает только origin `https://hainox.github.io`, а локальная проверка идёт с `127.0.0.1`. Поэтому вход префектуры и загрузка среза проверяются на опубликованной странице, а в автотестах служба подменяется стабом, который отдаёт корректные заголовки CORS.

### Боевые числа на 17.09.2026

Числа живые: районы продолжают отправлять наборы в течение дня, поэтому два замера разошлись. Это не расхождение выгрузки, а движение данных.

- Чтение отчёта в 12:50 МСК: в отчёте 16 районов, **маршрутов 21, зон 1, точек 16**, на приёмке **14**, утверждено **0**, отклонено **7**.
- Проверка выложенной страницы в 12:56 МСК: **маршрутов 24, зон 1, точек 16**, на приёмке **17**, утверждено **0**, отклонено **7**. Успели отправить Аэропорт (6 → 8 маршрутов) и Бескудниковский (3 → 4).
- Отправки помечены московским временем: последняя — 17.09 12:52 МСК.
- Без маршрутов 9 районов: Беговой, Войковский, Коптево, Левобережный, Молжаниновский, Савеловский, Тимирязевский, Ховрино, Хорошевский.

### Проверки

- Playwright `odh-map`: **31/31** (4 новых теста: блок в книге без сервиса, блок с загруженным срезом и приёмкой, CSV службы с BOM и именем, печатная форма с маршрутами). Срез подменяется стабом `https://api.test`, заголовки CORS отдаются — иначе проверка ловила бы отказ браузера, а не код.
- Корневые проверки атласа: pytest **27/27**, `hub/validate_hub.py` — OK.
- Живьём: вход префектуры и чтение отчёта по маршрутам с боевого сервиса; сайдбар и оба CSV сверены по скриншотам.
- **Проверка выложенной страницы после выкладки (коммит `a06da99`):** вход учётной записью префектуры с `https://hainox.github.io` прошёл, срез загрузился, поле пароля очистилось. В скачанной книге `sao-odh-register.xlsx` блок маршрутов стоит строкой 46: шапка на месте, 16 районов по убыванию маршрутов, времена отправок московские, у девяти районов без наборов число пустое. Файл службы скачался как `odh-routes-2026-09-17.csv`. Ошибок в консоли — **0**.

### Порядок выкладки и точки отката

1. `git push origin main` → GitHub Pages. Серверных изменений нет: карта статическая, сервис ОДХ уже отдаёт нужный эндпоинт и заголовки.
- Точка отката: коммит `1e79473` (выгрузки без маршрутов).

### Открытые наблюдения

- **Приёмка не разбирает наборы: 14 маршрутов на приёмке, утверждено 0.** Блок маршрутов это сразу показывает — вероятно, префектуре нужно время или доступ, стоит спросить заказчика.
- **Форма входа в публичном сайдбаре.** Пароль префектуры вводится на общедоступной странице карты; отдельной роли он не даёт (учётка одна), но если заказчик сочтёт это лишним, блок маршрутов логичнее перенести в «Приёмку» — код `exports.js` от места не зависит.
- Район и имя файла в отчёте службы — единственный источник для этого блока; при расхождении с `api/lib/route-report.js` правку нужно вносить там, а не в карте.




